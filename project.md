# Data cleaning

数据处理要求。

## 1. 项目范围与安全约束

- 本项目用于处理用户在终端交互界面中指定的源文件夹中的数据，本文将该文件夹记为`输入目录`。
- 仅处理`输入目录`直接包含的`.xlsx`文件，排除以`~$`开头的Excel临时文件；各类别的文件名筛选规则见对应章节。
- 处理程序已按本文要求实现，并通过全量自动化测试与样本核对验证；正式处理结果仅在用户通过终端界面选择数据类别并执行处理时生成。
- 处理程序使用Rust开发；`.xlsx`源文件使用`calamine`读取，`.xlsx`结果使用`rust_xlsxwriter`写入；日期与时间使用`chrono`处理，金额与比例使用`rust_decimal`按十进制处理，不使用二进制浮点数参与金额判断。终端交互界面使用Rust标准库`std::io`实现（`println!`输出、`stdin().read_line()`输入），不引入界面依赖。
- `输入目录`及其中所有文件均为只读数据源，不得修改、覆盖、移动、重命名或删除。
- 所有处理结果必须直接写入所选`输入目录`的父目录下的`输出目录`，不得写入`输入目录`。
- 所有输出数据文件均采用单表结构：每个Excel文件只包含一个工作表，不得增加汇总表、说明表或其他附加表。
- Excel输出工作表的标签名称不作指定，使用`rust_xlsxwriter`创建工作表时的默认名称，不得为此额外重命名。
- 源数据中的空值默认保持为空，不得写成`0`、`null`、`None`或其他占位内容。
- 除文档明确规定的转换外，不得自行清洗、修正、筛选或推断数据。
- 上述约束适用于全部数据组；各节只列补充或例外。

### 1.1 输入与输出路径

- 文中的`输入目录`是用户所选源文件夹的路径占位符，并非要求文件夹实际名为`输入目录`。处理程序须在终端交互界面提供源文件夹路径输入；绝对路径直接使用，相对路径以程序启动时的当前工作目录为基准（开发阶段在项目根目录执行`cargo run`时即为项目根目录），开始处理前解析为绝对路径并确认该路径存在、是文件夹且可读取。源文件夹可位于项目根目录内或外，不受程序所在目录限制。
- 路径输入时须去除首尾空白；如用户从文件管理器拖入路径导致首尾带有成对的单引号或双引号，应先去除该对引号再解析。
- 当前样本源文件位于项目根目录下的`data/`，仅用于核对现有数据；正式处理时不得默认固定读取此路径。
- 本文将所选`输入目录`的父目录下的`source_data`文件夹记为`输出目录`；该文件夹与`输入目录`同级，不存在时须自动创建。输出路径随用户所选源文件夹确定，与项目根目录、程序所在目录及当前工作目录无关。
- 所有数据组的结果文件直接写入`输出目录`，不另建年份子目录。
- 例如用户选择`/某路径/源数据/`时，发票结果写入`/某路径/source_data/发票明细.xlsx`；选择当前样本`data/`时，结果写入项目根目录下的`source_data/`。
- 每次处理只针对同一个年份的数据；所选类别中的年度源文件不得跨年度合并。
- 每个数据组使用固定输出基名及`.xlsx`扩展名。下表中的输入模式用于标明读取路径，文件名有效性和具体选择规则仍按各处理章节执行；`yyyy`、`yyyymmdd`及时间戳均为文件名中的对应数字：

| 数据组 | 输入路径或模式 | XLSX输出路径 |
|---|---|---|
| 发票明细 | `输入目录/发票_yyyymmdd.xlsx` | `输出目录/发票明细.xlsx` |
| 银联国补明细 | `输入目录/*银联国补明细*.xlsx` | `输出目录/银联交易明细所有.xlsx` |
| 数码已上传数据 | `输入目录/MER_89813014812B06R_*.xlsx` | `输出目录/已上传数码.xlsx` |
| 家电、电脑已上传数据 | `输入目录/MER_89813015722APT1_*.xlsx` | `输出目录/已上传家电电脑.xlsx` |
| 门店银联交易明细 | `输入目录/89813014812B06R_MX_YYYYMMDDHHMMSS_序号.xlsx`及`输入目录/89813015722APT1_MX_YYYYMMDDHHMMSS_序号.xlsx` | `输出目录/银联交易明细门店.xlsx` |
| 家电电脑回款明细 | `输入目录/yyyy年以旧换新补贴明细.xlsx` | `输出目录/回款明细家电电脑.xlsx` |
| 数码回款明细 | `输入目录/yyyy年数码补贴明细.xlsx` | `输出目录/回款明细数码.xlsx` |
| 收款单统计 | `输入目录/收款单统计.xlsx` | `输出目录/收款单统计.xlsx` |
| 销售用券情况统计 | `输入目录/销售用券情况统计.xlsx` | `输出目录/销售用券情况统计.xlsx` |


### 1.2 终端交互界面

- 处理程序提供简单的终端文字界面：使用标准库`println!`显示提示和菜单，使用`std::io::stdin().read_line()`接收源文件夹路径与类别编号；界面不引入第三方依赖，数据处理依赖不受此限制。
- 终端输出与输入均使用UTF-8；Windows环境下须确保中文提示正常显示，不得因控制台编码导致路径或提示乱码。
- 每次运行先提示用户输入源文件夹路径。路径为空、不存在、不是文件夹或不可读取时，显示具体原因并重新提示，不得开始处理。
- 路径有效后显示以下编号菜单，由用户输入一个编号选择本次要处理的数据类别；不输入内容直接回车时，按编号1至9的顺序依次执行全部类别：

| 编号 | 数据类别 | 对应处理章节 |
|---:|---|---|
| 1 | 发票明细 | 第3节 |
| 2 | 银联国补明细 | 第4节 |
| 3 | 数码已上传数据 | 第5节 |
| 4 | 家电、电脑已上传数据 | 第5节 |
| 5 | 门店银联交易明细 | 第6节 |
| 6 | 家电电脑回款明细 | 第7节 |
| 7 | 数码回款明细 | 第8节 |
| 8 | 收款单统计 | 第9节 |
| 9 | 销售用券情况统计 | 第10节 |

- 输入无效编号时显示提示并重新要求选择。
- 输入1至9中任一编号时，每次只执行该类别，并仅生成该类别规定的XLSX文件；但部分类别处理时还须从同一`输入目录`只读地引用其他类别的数据源用于校验或匹配，不因此生成被引用类别各自的输出文件：
  - 选择数码已上传数据或家电、电脑已上传数据（编号3、4）时，还须按第5.7节匹配对应的回款明细（数码↔第8节回款明细数码，家电、电脑↔第7节回款明细家电电脑）。
  - 选择销售用券情况统计（编号9）时，还须依次读取：第10.6.1节的门店银联交易明细（建立参考号校验集）、第10.10节的发票明细（匹配数电发票号码）、第10.12.1节的收款单统计（匹配备注）、第10.12.2节的已上传家电电脑及已上传数码（备注兜底匹配，其自身又按上一条规则各自引用对应的回款明细）。
- 不输入内容直接回车（空输入）时，按编号1至9的顺序依次执行全部类别；其中某一类别处理失败不得中断其余类别，须继续处理后续类别，并分别报告每个类别各自的成功或失败结果。单个类别内部发生的程序级错误（包括panic）也须被捕获并作为该类别的失败结果报告，不得导致整个程序退出。
- 处理成功后在终端显示本次类别及输出文件的完整路径；处理失败时显示具体错误，不将失败结果作为正式输出。无论执行单个类别还是空输入执行全部类别，处理完成后程序默认自动退出，不返回类别菜单。
- 输入源文件夹路径或菜单编号阶段，若用户通过终端中断信号（`Ctrl+C`）或输入结束（Linux/macOS为`Ctrl+D`，Windows为`Ctrl+Z`后回车）终止输入，程序同样正常退出；`read_line()`返回读取字节数为0时视为输入结束。

### 1.3 开发环境与结果发布

- 项目目录与模块职责见[architecture.md](architecture.md)；本文各数据组的处理和验收规则为实现依据。
- 开发基准为Rust 1.98.1（截至2026年9月17日的最新稳定版，不使用beta或nightly工具链），Edition 2024。项目根目录的`rust-toolchain.toml`固定该工具链版本；`Cargo.toml`声明直接依赖，`Cargo.lock`锁定完整依赖并纳入版本控制。开发、测试、构建与验收须使用同一锁文件。
- 直接依赖及版本如下（均为截至2026年9月17日的最新稳定版）：

| 依赖 | 版本 | 用途 |
|---|---|---|
| `calamine` | 0.36.1 | 读取`.xlsx`源文件（启用`dates`特性） |
| `rust_xlsxwriter` | 0.99.1 | 写入`.xlsx`结果（启用`chrono`、`rust_decimal`特性） |
| `chrono` | 0.4.45 | 日期、日期时间、时间解析与格式化 |
| `rust_decimal` | 1.43.0 | 金额与比例的十进制运算 |
| `regex` | 1.13.1 | 文件名与备注、摘要文本匹配 |
| `thiserror` | 2.0.20 | 统一错误类型定义 |

- 首次准备环境时，安装`rustup`后在项目根目录执行任意`cargo`命令，`rustup`会按`rust-toolchain.toml`自动安装指定工具链。常用命令均在项目根目录执行：
  - 开发运行：`cargo run --locked`
  - 运行测试：`cargo test --locked`
  - 代码检查：`cargo clippy --locked --all-targets`与`cargo fmt --check`
  - 发布构建：`cargo build --release --locked`
- 程序入口为`src/main.rs`；发布构建产物为`target/release/data-cleaning`（Windows为`data-cleaning.exe`），可单独复制到其他电脑运行，无需安装Rust或其他运行环境。`target/`目录不纳入版本控制。
- 读取源表时必须按Excel绝对行号、列号定位单元格。`calamine`返回的数据区域从第一个非空单元格开始，读取模块须使用区域起始偏移（`Range::start()`）换算回绝对位置，确保开头空白行、列不会导致本文指定的表头行号和列位置发生偏移；“最后一个实际有值行”须按实际非空单元格计算，不得依赖区域边界或格式化范围。按本文规定生成XLSX格式与填色，不依赖复制源工作簿样式。
- 源单元格可能为文本、数值、布尔、Excel日期序列值或错误值；读取模块须保留原始单元格类型供处理模块判断。遇到Excel错误值（如`#N/A`、`#REF!`）且该单元格属于必需输出字段时，按数据异常终止该类别处理并报告位置。
- 每次只发布当前选中类别规定的一份XLSX结果。目标文件已存在时，处理成功后直接覆盖，无需再次询问；其他类别的结果文件不得因本次处理而改变。
- 先在`输出目录`准备本次XLSX临时文件，完成该类别的全部校验，并确认临时文件已成功生成，再替换正式目标文件。替换前保留已有结果；生成、校验或替换任一步骤失败时，恢复或保留原有结果，清理本次临时文件。
- 正式目标文件被其他程序（如Excel）占用而无法替换时，按发布失败处理，恢复原有结果并提示用户关闭占用文件后重试。

## 2. 数据源

### 2.1 发票明细

- 内容：发票明细数据。
- 文件名格式：`发票_yyyymmdd.xlsx`，其中`yyyy`为四位年份、`mm`为两位月份、`dd`为两位日期。
- 当前文件：`输入目录/发票_20260914.xlsx`。
- 当前工作表：`发票_20260914`。
- 第6行为表头，第7行起为明细；表头前的标题和空白行不属于数据。
- 源表字段按列顺序如下：

  1. `订单号`
  2. `创建时间`
  3. `开票时间`
  4. `开票类型`
  5. `发票种类`
  6. `发票性质`
  7. `发票代码`
  8. `发票号码`
  9. `数电发票号码`
  10. `购方名称`
  11. `购方税号`
  12. `购方手机号`
  13. `购方邮箱`
  14. `购方开户行及账号`
  15. `购方地址、电话`
  16. `主要商品名称`
  17. `合计含税金额`
  18. `税率`
  19. `合计不含税金额`
  20. `合计税额`
  21. `备注信息`
  22. `部门门店`
  23. `开票方式`
  24. `开票员`
  25. `收款人`
  26. `复核人`
  27. `PDF地址`
  28. `开票状态`
  29. `操作人`
  30. `打印状态`

### 2.2 银联国补明细

- 内容：银联国补交易明细数据。
- 文件识别规则：文件位于`输入目录`、扩展名为`.xlsx`，且文件名中包含连续文字`银联国补明细`；该文字前后均可存在其他字符。
- 当前文件：`银联国补明细1.xlsx`至`银联国补明细12.xlsx`，共12个工作簿；该数量仅作为当前数据核对基准，不构成固定处理范围。
- 当前12个工作簿共22个工作表，所有工作表的字段结构一致，第1行为表头，第2行起为明细；当前共有84,907条明细记录，不含各工作表的表头行。
- 后续新增且符合上述文件识别规则的工作簿及其中全部工作表也属于本数据组，必须自动纳入处理；不得硬编码当前12个工作簿、22个工作表或当前文件名清单。
- 源表字段按列顺序如下：

  1. `银商订单号`
  2. `银商原订单号`
  3. `交易类型`
  4. `交易日期`
  5. `交易时间`
  6. `支付方式`
  7. `订单金额`
  8. `应收金额`
  9. `商品名称`
  10. `商品明细`
  11. `品牌`
  12. `商品类别`
  13. `能耗等级`
  14. `商品型号`
  15. `商品识别码SN`
  16. `IMEI1`
  17. `IMEI2`
  18. `备注`
  19. `交易参考号`
  20. `商家优惠金额`
  21. `渠道优惠（云闪付、宝信）`
  22. `活动ID`
  23. `实收金额`
  24. `银商商户号`
  25. `银商商户名称`
  26. `终端号`
  27. `门店编码`
  28. `门店名称`
  29. `小U交易流水号`
  30. `小U原交易流水号`
  31. `订单号`
  32. `订单流水号`
  33. `收银员`

### 2.3 数码大类已上传明细（B06R）

- 内容：数码大类的已上传明细；当前工作表标题为“以旧换新数据[手机/其他3C]”。
- 文件名匹配`MER_89813014812B06R_*.xlsx`（前缀+扩展名，`*`部分不限定内容或位数，规则见第5.1节）。当前文件：对应的8个工作簿，`*`部分当前为14位导出时间戳（范围`20260914101809`至`20260914101921`）加固定后缀`yjhx`；该具体形式、文件数量和时间范围仅作为当前数据核对基准。后续新增的匹配工作簿须自动纳入处理，不得硬编码当前范围。
- 当前每个工作簿均包含一个名为`手机其他3C`的工作表。
- 第1行为合并标题，第2行为表头，第3行起为明细；标题行和表头行均不得作为明细数据。
- 当前8个工作表结构一致，共58列、7,185条明细记录。
- 第23列和第28列的名称均为`图片1`，属于两个不同字段；读取和处理时必须按列位置区分，不得仅按字段名称合并或覆盖。
- 源表字段按列顺序如下：

  1. `实时清分UUID`
  2. `商户号`
  3. `商户名称`
  4. `订单号`
  5. `交易日期`
  6. `交易金额`
  7. `检索参考号`
  8. `模版类型`
  9. `状态`
  10. `描述`
  11. `提交时间`
  12. `更新时间`
  13. `终端号`
  14. `分店id`
  15. `分店名`
  16. `所在地区`
  17. `详细地址`
  18. `地区编码`
  19. `tel`
  20. `发票号码`
  21. `发票金额`
  22. `购买方名称`
  23. `图片1`（第一处）
  24. `S/N码`
  25. `是否属于 AI 产品`
  26. `IMEI1`
  27. `IMEI2`
  28. `图片1`（第二处）
  29. `图片2`
  30. `图片3`
  31. `图片4`
  32. `图片5`
  33. `图片6`
  34. `img7`
  35. `img8`
  36. `img9`
  37. `img10`
  38. `img11`
  39. `img12`
  40. `img13`
  41. `img14`
  42. `img15`
  43. `签收时间`
  44. `remark`
  45. `物流单号`
  46. `erpOrderNum`
  47. `ocrModify`
  48. `modifyStatus`
  49. `introduceInvoiceFlag`
  50. `是否交旧`
  51. `是否自提`
  52. `receiverName`
  53. `productCode`
  54. `subsideAmt`
  55. `productName`
  56. `oldExchangeType`
  57. `收货地址是否农村地区`
  58. `开票日期`

### 2.4 家电、电脑已上传数据明细（APT1）

- 内容：家电、电脑大类的已上传数据明细。
- 文件名匹配`MER_89813015722APT1_*.xlsx`（前缀+扩展名，`*`部分不限定内容或位数，规则见第5.1节）。当前文件：对应的9个工作簿，`*`部分当前为14位导出时间戳（范围`20260914102009`至`20260914102128`）加固定后缀`yjhx`；该具体形式、文件数量和时间范围仅作为当前数据核对基准。后续新增的匹配工作簿须自动纳入处理，不得硬编码当前范围。
- 当前9个工作簿共包含10个工作表，均以第1行为合并标题、第2行为表头、第3行起为明细；当前共4,825条明细记录。
- 工作表分为两种结构：

| 工作表类型 | 工作表数量 | 明细数量 | 列数 | 结构说明 |
|---|---:|---:|---:|---|
| `家电` | 9 | 4,824 | 58 | 第26列起为图片字段，并包含`airConditionerKitInfo` |
| `电脑` | 1 | 1 | 58 | 第26列为`电脑类型`，后续图片字段相对家电表发生位移，且不含`airConditionerKitInfo` |

- 两种结构的第1至第25列相同，依次为：`实时清分UUID`、`商户号`、`商户名称`、`订单号`、`交易日期`、`交易金额`、`检索参考号`、`模版类型`、`状态`、`描述`、`提交时间`、`更新时间`、`终端号`、`分店id`、`分店名`、`所在地区`、`详细地址`、`地区编码`、`tel`、`发票号码`、`发票金额`、`购买方名称`、`图片1`、`S/N码`、`是否属于 AI 产品`。
- 两类工作表均为58列，但第26列起不能仅按列号直接对应；后续如需跨类型合并，必须依据字段名称及已确认的字段映射处理。

### 2.5 门店银联交易明细

- 内容：门店银联交易明细数据。
- 业务文件名结构：`商户号_MX_YYYYMMDDHHMMSS`，其中`YYYYMMDDHHMMSS`为14位年月日时分秒时间戳。
- 实际输入文件完整格式为`商户号_MX_YYYYMMDDHHMMSS_序号.xlsx`；时间戳随导出时间变化，序号用于区分同组导出文件，不得限定为当前时间戳或当前序号范围。
- 当前范围包含以下两组文件，数量仅作为当前数据核对基准：
  - `89813014812B06R_MX_*.xlsx`：9个工作簿，共7,581条交易明细。
  - `89813015722APT1_MX_*.xlsx`：9个工作簿，共6,390条交易明细。
- 当前两组合计18个工作簿、13,971条交易明细。
- 后续新增且商户号属于上述两组、文件名完整符合`商户号_MX_YYYYMMDDHHMMSS_序号.xlsx`格式的工作簿也必须自动纳入所属数据组；不得硬编码当前18个工作簿、当前文件名、时间戳或序号范围。
- 当前每个工作簿均包含一个名为`对账数据`的工作表，字段结构一致。
- 第1行为汇总信息，第2行为表头，第3行至倒数第2行为交易明细，最后一行为D1手续费提示。汇总行、表头行和末尾提示行均不得作为交易明细。
- 源表共26个字段，按列顺序如下：

  1. `清算时间`
  2. `交易时间`
  3. `终端号`
  4. `交易类型`
  5. `卡号`
  6. `交易金额`
  7. `清算金额`
  8. `手续费`
  9. `T0手续费`
  10. `D1手续费`
  11. `流水号`
  12. `检索号`
  13. `卡类型`
  14. `发卡行`
  15. `商户号`
  16. `商户名称`
  17. `分店简称`
  18. `商户订单号`
  19. `银商订单号`
  20. `交易方式`
  21. `分店`
  22. `优惠金额`
  23. `分期手续费`
  24. `付款附言`
  25. `备注`
  26. `买家ID`

### 2.6 家电、电脑以旧换新补贴明细

- 通用文件名格式：`yyyy年以旧换新补贴明细.xlsx`，其中`yyyy`为四位年份；当前文件为`2026年以旧换新补贴明细.xlsx`。
- 内容：对应年份的家电、电脑等以旧换新补贴拨付批次明细及按门店汇总数据。
- 每次只处理一个年份的工作簿及其全部批次明细，不得跨年度合并。
- 当前工作簿共包含8个工作表，其中7个为批次明细表，1个为`汇总`表；当前批次数量和工作表数量仅作为数据核对基准。
- 当前7个批次明细表均以第1行为表头、第2行起为明细，合计104,029条明细记录。
- 后续新增的批次明细工作表也属于本数据组，必须按第7节的批次识别规则自动纳入处理；不得硬编码当前7个批次、当前工作表数量或当前工作表名称清单。
- 名为`汇总`的工作表始终属于汇总数据，不作为逐笔补贴明细处理。
- 部分工作表被设置了整行或整列格式，工作表范围可能显示至Excel最大行数或较远列；没有实际值的格式化空白单元格不属于数据，不得计为明细或字段。
- `汇总`表为按商户编号、门店和批次统计的公式汇总表，不属于逐笔补贴明细。

| 工作表 | 类型 | 字段数 | 当前明细数 |
|---|---|---:|---:|
| `2026年家电过渡期第一批次（1.1.-1.25）` | 批次明细 | 20 | 4,486 |
| `2026年家电过渡期第二批次（1.1.-1.29）` | 批次明细 | 20 | 5,737 |
| `1.1-1.29` | 批次明细 | 21 | 15,738 |
| `2026年家电第四批次（1.30-4.28）` | 批次明细 | 21 | 3,649 |
| `2026年家电第五批次（1.30-6.28）` | 批次明细 | 22 | 16,407 |
| `2026年家电第6批次（2.1-7.7）` | 批次明细 | 24 | 30,292 |
| `2026年家电第7批次（7.19-8.4）` | 批次明细 | 25 | 27,720 |
| `汇总` | 汇总数据 | 非统一明细结构 | 不计入明细 |

批次明细表的结构随批次发生变化：

- 第一批次为20列，表头依次为：`拨付批次`、`交易时间`、`交易参考号`、`商户订单号`、`交易订单号`、`销方名称`、`商户编号`、`应收销售金额`、`其他支付`、`实收销售金额`、`补贴金额`、`补贴比例`、`SN码`、`所在地区`、`商品编码`、`能耗等级`、`编码品类`、`商品名称`、`发票号`、`ID`。
- 第二批次为20列，将部分表头调整为`交易完成时间`、`销售企业名称`、`核销商编`和`销售金额`，仍不包含`发票金额`。
- `1.1-1.29`和第四批次为21列，字段顺序为：`拨付批次`、`交易完成时间`、`交易参考号`、`商户订单号`、`交易订单号`、`销售企业名称`、`核销商编`、`其他支付`、`销售金额`、`实收销售金额`、`补贴金额`、`补贴比例`、`SN码`、`所在地区`、`商品编码`、`能耗等级`、`编码品类`、`商品名称`、`发票金额`、`发票号`、`ID`。
- 第五批次在上述21列之后新增`原拨付批次`，共22列。
- 第6批次在21列结构中于`发票号`之后新增`发票头/购买方名称`，并在`ID`之后新增`退回原因`和`原拨付批次`，共24列。
- 第7批次在第6批次结构基础上，于`交易完成时间`之前新增`创建时间`，共25列。
- 第一、第二批次与后续批次存在同义但名称不同的字段；如何统一字段名称、是否合并7张明细表以及`汇总`表是否参与输出，须按后续处理要求执行，不得自行推断。

### 2.7 数码补贴明细

- 内容：对应年份的数码补贴各批次回款明细及按门店汇总数据。
- 通用文件名格式为`yyyy年数码补贴明细.xlsx`，其中`yyyy`为四位年份；当前输入文件为`输入目录/2026年数码补贴明细.xlsx`。
- 当前工作簿包含4个批次明细工作表和1个`汇总`工作表；当前有效明细共63,737条。
- 名为`汇总`的工作表为统计汇总表，不属于逐笔明细，处理时必须完全排除。
- 当前批次工作表名称、表头列数与各自明细数仅用作核对基准，不构成固定批次范围；批次识别规则、字段映射与处理要求见第8节。

### 2.8 收款单统计

- 文件名固定为`收款单统计.xlsx`，当前文件为`输入目录/收款单统计.xlsx`。
- 当前工作簿包含一个名为`Sheet1`的工作表，共60个字段。
- 第1行为标题行，第2行为表头，第3行起为数据区域；最后一个实际有值行是合计行，不属于明细。
- 当前合计行位于第20,604行；排除标题行、表头行和合计行后，共有20,601条有效明细。

### 2.9 销售用券情况统计

- 内容：销售用券情况明细与补贴核对数据。
- 文件名固定为`销售用券情况统计.xlsx`，当前文件为`输入目录/销售用券情况统计.xlsx`。
- 当前工作簿包含一个名为`Sheet1`的工作表，共28个源字段。
- 第1行为标题行，标题为`销售用券情况统计`；第2行为表头，第3行起为明细数据；最后一个实际有值行是合计行，不属于明细。
- 当前合计行位于第16,621行；排除标题行、表头行和合计行后，第3行至第16,620行共有16,618条有效明细。
- 详细源字段与提取规则见第10节。

## 3. 发票明细处理

### 3.1 输入与输出

- 在`输入目录`中识别全部完整符合`发票_yyyymmdd.xlsx`格式的文件，其中`yyyymmdd`必须能够解析为有效日期。
- 仅选择文件名日期最新的一个工作簿作为本次输入，不合并较早日期的发票文件；当前最新输入文件为`输入目录/发票_20260914.xlsx`。
- 输入工作簿必须且只能包含一个有效发票明细工作表；其第6行表头须与第2.1节规定的30个字段名称及顺序完全一致，第7行起为全部明细。
- 文件名日期无效、最新日期无法唯一确定、工作表数量异常或第6行表头不符合要求时，必须终止处理，不得生成正式输出，并报告文件名、工作表名及具体异常。
- 处理结果输出为：
  - `发票明细.xlsx`
- 输出文件只包含一个处理后的数据表。
- Excel文件保留本节要求的整行填充颜色。

### 3.2 输出字段与类型

仅输出以下8个字段，并严格保持顺序：

| 顺序 | 字段 | Excel保存类型 | 要求 |
|---:|---|---|---|
| 1 | `开票时间` | 日期时间，显示格式`yyyy-mm-dd HH:mm:ss` | 保留到秒 |
| 2 | `开票类型` | 文本 | 保持原值 |
| 3 | `数电发票号码` | 文本 | 不得转为数值或科学计数法，不得截断或丢失前导零 |
| 4 | `购方名称` | 文本 | 保持原值 |
| 5 | `主要商品名称` | 文本 | 保存清洗后的值 |
| 6 | `备注信息` | 文本 | 保持原文，包括标点、空格和换行 |
| 7 | `开票状态` | 文本 | 保持原值 |
| 8 | `匹配单据号` | 文本 | 按本节规则生成，空值保持为空 |

- 发票输出中没有按数值类型保存的字段。

### 3.3 主要商品名称

- 以最后一个半角星号`*`为分隔符，删除该星号及其之前的全部内容，仅保留星号后的商品名称。
- 原值不含`*`时保持不变。
- 示例：`*家用清洁电器具*小天鹅-洗衣机-TG12TP3` → `小天鹅-洗衣机-TG12TP3`。

### 3.4 匹配单据号

`匹配单据号`的格式为`yymmdd单据号`，日期与单据号直接拼接，不加空格或分隔符。

按以下顺序处理：

1. 解析备注：兼容半角冒号、全角冒号、顿号、空格、换行及无分隔符的写法。解析过程不得改变输出的`备注信息`原文。
2. 提取日期：
   - `销售日期`和`购机日期`视为同一字段，仅提取明确带有这两个标签之一的日期。
   - 同一日期重复出现时按一个日期处理；出现多个不同日期时不作选择，`匹配单据号`留空。
   - 备注中同时出现开具购物发票日期时，不使用该开票日期。
3. 标准化日期：
   - 月份和日期可为一位或两位，一位时自动补零，例如`2026-8-9` → `260809`。
   - 两位年份的点分日期按2000年代处理，例如`26.02.16` → `260216`。
   - 兼容连接符与中文年月日混用，例如`2026-2月21日` → `260221`。
   - 执行已确认的定向修正：`2026-0101` → `2026-01-01`；`202601-04` → `2026-01-04`；`2026-26-29` → `2026-06-29`。
4. 提取单据号：
   - `单据号`和`单据收款号`视为同一字段。
   - 删除开头的`收款`二字；没有该前缀时直接使用原值。
   - 单据号与前后文字紧邻时，不得把后续说明文字计入单据号。
   - 同一个单据号重复出现时按一个处理；出现多个不同单据号时不作选择，`匹配单据号`留空。
5. 标准化并校验单据号：

   | 原始情况 | 处理规则 |
   |---|---|
   | 10位 | 直接使用 |
   | 11位 | 从连续补零段中删除一个多余的`0`，纠正为10位 |
   | 9位且为`ZFP300008` | 在补零段增加一个`0`，纠正为`ZFP3000008` |
   | 其他9位或其他无法得到10位的内容 | 判定无效 |

   11位纠正示例：`ZHLT0000524` → `ZHLT000524`；`ZFTY0000027` → `ZFTY000027`。
6. 生成结果：日期转换为`yymmdd`后，与10位标准单据号直接拼接。例如，`销售日期:2026-07-28`和`单据号:收款ZEXQ000062`生成`260728ZEXQ000062`。

以下任一情况发生时，`匹配单据号`留空：

- 备注为空或仅包含红冲等无关说明；
- 缺少销售日期、购机日期或单据号；
- 日期无法识别且不属于已确认的定向修正；
- 出现多个不同日期；
- 出现多个不同单据号；
- 单据号处理后仍不是10位。

空`匹配单据号`不参与重复判断，多个空值不得视为重复。

### 3.5 分类、排序与填色

处理优先级为：先识别异常记录，再在剩余记录中判断重复。异常记录不参与重复判断。

| 分组 | 判定条件 | 输出位置 | Excel整行填充 |
|---|---|---|---|
| 异常记录 | `开票类型=红票`，或`开票状态=已红冲`，或`开票状态=开票失败` | 最底部 | 粉色`#FFC7CE` |
| 正常记录 | 排除异常记录后的剩余记录（即上述三个条件均不满足）；非空`匹配单据号`不重复，或`匹配单据号`为空 | 最上部 | 无特殊颜色 |
| 重复记录 | 排除异常记录后的剩余记录中，非空`匹配单据号`重复的全部记录 | 正常记录之后 | 黄色`#FFEB9C` |

- 判定顺序为先排除异常记录，再在剩余记录中按`匹配单据号`是否重复分为正常、重复两组；剩余记录不要求额外满足`开票类型=蓝票`且`开票状态=开票完成`——只要不落入异常记录的三个条件，即按此规则处理。
- 同一记录同时满足异常和重复条件时，只归入异常记录并填充粉色`#FFC7CE`。
- 最终顺序固定为：正常记录 → 重复记录 → 异常记录。
- 各分组内部不再额外排序，保持源数据中的相对顺序。
- 当前数据中的`红票+开票完成`、`蓝票+已红冲`、`蓝票+开票失败`和`蓝票+开票完成`四种组合均已覆盖；源数据出现上述四种组合之外的`开票类型`/`开票状态`搭配时，按本节排除法归入正常或重复记录，不视为结构异常。

### 3.6 验收要求

- 输出文件仅包含规定的8个字段，字段名称和顺序一致。
- 输出文件的明细数量与输入明细数量一致，且数据和排列顺序一致。
- 当前源文件不变时，应选择`输入目录/发票_20260914.xlsx`并输出15,516条明细；出现更新日期的匹配文件后，应仅处理最新日期文件，并按该文件的实际有效明细数验收。
- `主要商品名称`按最后一个`*`正确清洗，`备注信息`保持原文。
- `匹配单据号`严格按照日期提取、单据号纠正及留空规则生成，并按文本保存。
- 空`匹配单据号`不参与重复判断。
- Excel中的重复记录整行填充黄色`#FFEB9C`、异常记录整行填充粉色`#FFC7CE`，三类记录顺序正确。

## 4. 银联国补明细处理

### 4.1 输入与合并

- 读取`输入目录`中所有扩展名为`.xlsx`且文件名中包含连续文字`银联国补明细`的工作簿及其全部工作表。
- 处理范围不得仅限当前12个工作簿；后续新增且符合上述识别规则的工作簿及其中全部工作表须自动纳入，不得设置固定文件数、工作表数或明细行数上限。
- 每个工作表第1行为表头，第2行起为明细。
- 每个工作表必须包含数据源说明中的33个字段，字段名称和顺序必须一致。
- 任一匹配工作表的第1行无法完整匹配33个字段，或存在缺失、重复、额外、错位字段时，必须终止本次处理，不得生成正式输出，并报告文件名、工作表名及具体字段异常。
- 按完整文件名执行自然排序：连续数字按数值大小比较，其他文字按字符顺序比较；例如`银联国补明细2.xlsx`排在`银联国补明细10.xlsx`之前。该规则同样适用于文字或混合后缀，确保每次处理顺序确定且可复现。
- 同一工作簿内按原工作表顺序读取。
- 将所有明细纵向追加为一个结果表，只保留一行表头；源工作表的表头不得作为明细写入。
- 保留全部33个字段及全部源明细，不新增或删除字段，不筛选、不去重。
- 完成合并后，应检查全部非空`小U交易流水号`和非空`订单号`。任一字段内出现重复值，或两个不同文件包含完全相同的表头及全部明细内容时，视为疑似重复导出，必须终止处理并报告涉及文件和重复值；不得自行删除其中一份数据后继续输出。

### 4.2 字段标准化

仅对`品牌`和`商品类别`执行以下精确值替换。

品牌：

| 原值 | 标准值 |
|---|---|
| `Leader` | `统帅` |
| `leader` | `统帅` |
| `A.O.史密斯` | `AO史密斯` |

商品类别：

| 原值 | 标准值 |
|---|---|
| `热水器` | `厨卫` |
| `电视机` | `电视` |
| `电冰箱` | `冰箱` |

未列出的`品牌`和`商品类别`值保持不变，其他31个字段均保持原值。

### 4.3 字段保存格式

| 类型 | 字段 | Excel保存要求 |
|---|---|---|
| 日期 | `交易日期` | 日期类型，显示格式`yyyy-mm-dd` |
| 时间 | `交易时间` | 时间类型，24小时制`HH:mm:ss`显示格式，保留到秒 |
| 数值 | `订单金额`、`应收金额`、`商家优惠金额`、`渠道优惠（云闪付、宝信）`、`实收金额` | 数值类型，不添加货币符号或千位分隔符，不使用科学计数法 |
| 文本 | 其余26个字段 | 文本类型 |

必须按文本保存的26个字段为：

1. `银商订单号`
2. `银商原订单号`
3. `交易类型`
4. `支付方式`
5. `商品名称`
6. `商品明细`
7. `品牌`
8. `商品类别`
9. `能耗等级`
10. `商品型号`
11. `商品识别码SN`
12. `IMEI1`
13. `IMEI2`
14. `备注`
15. `交易参考号`
16. `活动ID`
17. `银商商户号`
18. `银商商户名称`
19. `终端号`
20. `门店编码`
21. `门店名称`
22. `小U交易流水号`
23. `小U原交易流水号`
24. `订单号`
25. `订单流水号`
26. `收银员`

数据完整性要求：

- 除已规定的品牌和商品类别替换外，其他字段值保持不变。
- 订单号、流水号、商户号、终端号、门店编码、活动ID、SN和IMEI等标识字段不得转为科学计数法，不得截断、四舍五入或丢失前导零。
- 数值字段为空时保持为空，不得自动转换为`0`。
- 合并和字段标准化不得增加或减少明细记录。

### 4.4 输出文件

合并结果输出为：

- `银联交易明细所有.xlsx`

输出要求：

- 输出文件只包含一个合并后的数据表，且只有一行表头。

### 4.5 验收要求

- 必须成功读取全部符合命名规则的工作簿及其全部工作表；当前源文件不变时，应读取12个工作簿、22个工作表，新增文件后按实际数量验收。
- 输出表仅有一行表头，33个字段的名称和顺序与源表一致。
- 当前源文件不变时，输出文件应包含84,907条明细记录；新增文件后，输出明细数必须等于全部已纳入工作表的有效明细数之和。
- `Leader`、`leader`和`A.O.史密斯`已全部替换，原值不再出现。
- `热水器`、`电视机`和`电冰箱`已全部替换，原值不再出现。
- 输出数据的日期、时间、数值及文本字段符合保存类型要求。
- 所有标识字段保持完整文本值，空值保持为空。
- 当前数据的非空`小U交易流水号`和非空`订单号`均不得出现重复；新增文件后如发现重复值或内容完全相同的重复导出文件，必须按第4.1节终止处理，不得生成正式输出。

## 5. 已上传数据处理

本节统一规定“数码已上传数据”和“家电、电脑已上传数据”的处理方式。两组数据分别处理、分别输出，不得相互混合。

### 5.1 文件识别、数据分组与有效行

- 已上传数据的文件名识别规则见下表；`*`为任意后续字符（不限定时间戳位数或`yjhx`等固定后缀），文件名以对应前缀开头、以`.xlsx`结尾即视为匹配。文件名以`~$`开头的Excel临时文件必须排除。

| 数据组 | 商户号 | 文件名识别规则 | 当前工作簿/工作表 | 当前明细数 | 输出文件 |
|---|---|---|---:|---:|---|
| 数码已上传数据 | `89813014812B06R` | `MER_89813014812B06R_*.xlsx` | 8个工作簿、8个工作表 | 7,185 | `已上传数码.xlsx` |
| 家电、电脑已上传数据 | `89813015722APT1` | `MER_89813015722APT1_*.xlsx` | 9个工作簿、10个工作表 | 4,825 | `已上传家电电脑.xlsx` |

- 上表中的当前工作簿数、工作表数和明细数仅作为当前源文件的验收基准，不构成固定处理范围；不得为任一数据组设置固定工作簿数、工作表数或明细行数。
- 两个数据组均不得仅限当前工作簿；后续新增且文件名符合上表对应前缀及扩展名的全部工作簿及其中全部工作表必须自动纳入所属数据组。

有效数据范围：

- 读取每个匹配工作簿中的全部工作表。
- 每个工作表第1行为标题、第2行为表头、第3行起为明细；标题行和表头行不得作为数据。
- 两组数据组的源工作表均为58列结构，且均输出全部58列源字段（第5.3、5.4节），不再区分“公共字段”和按名称选取的子集；此外按第5.7节匹配或估算新增第59列`补贴金额`，该列不来自源工作表。
- 前25列两组结构相同且列位置固定：第2行前25列必须与第5.3节第1–25列的字段名称按列位置逐一完全一致，不一致时按结构异常终止处理。
- 第26列起两组结构不同（数码在此处插入`IMEI1`、`IMEI2`）；同一数据组内部还存在表头变体（如第2.4节的“电脑”工作表），因此第26列起按字段名称在该范围内查找，不依赖固定列号：
  - 各字段的已确认候选名称见第5.3、5.4节；`img5`与`图片5`视为同一字段的同义名称（“电脑”工作表使用后者）。
  - `airConditionerKitInfo`为已知的可选字段：“电脑”工作表没有该字段，此时对应输出值留空，不视为结构异常。
  - 第26列起除上述已知同义名称和可选字段外，任一必需字段在该范围内缺失，或同一名称在该范围内出现两次以上无法唯一确定列号，均按结构异常终止处理。
- 两个`图片1`为源表同名字段：第23列的`图片1`属于前25列固定结构；第二个`图片1`（家电、电脑组第26列或“电脑”工作表第27列，数码组第28列）按上述名称查找规则在“第26列起”范围内单独查找，因限定查找范围而不会与第23列混淆。
- 出现说明页、汇总页等非明细工作表，或前25列结构不符时，必须终止本次处理，不得生成正式输出，并报告文件名、工作表名及具体结构异常。

### 5.2 合并规则

每个数据组分别按以下规则合并：

1. 按文件名升序读取工作簿。
2. 同一工作簿内按原工作表顺序读取。
3. 将所有明细纵向追加为一个结果表。
4. 结果表只保留一行表头。
5. 保留该数据组的全部源明细，不筛选、不去重。
6. 两个数据组独立合并，数码数据不得写入家电、电脑结果，家电、电脑数据也不得写入数码结果。
7. 前25列输出字段按第5.1节规定的固定列位置读取；第26列起按字段名称在该范围内查找（第5.1节），已知的同义名称和可选字段按该节规则处理，其余情况下字段缺失、名称无法唯一确定或结构无法识别时按结构异常终止处理。
8. 每个数据组完成合并后，检查全部非空`实时清分UUID`；同一数据组中出现重复值，或两个不同文件包含完全相同的标题、表头及全部明细内容时，视为疑似重复导出，必须终止处理并报告涉及文件和重复值，不得自行去重后继续输出。

### 5.3 家电、电脑已上传数据输出字段及保存格式

家电、电脑已上传数据固定输出以下59列，并严格保持名称、顺序和保存格式；第1至58列取自源工作表，第59列`补贴金额`按第5.7节匹配或估算生成：

| 顺序 | 字段 | Excel保存类型 | 要求 |
|---:|---|---|---|
| 1 | `实时清分UUID` | 文本 | 保留完整原值，不得使用科学计数法或截断 |
| 2 | `商户号` | 文本 | 保留完整原值，不得丢失前导零 |
| 3 | `商户名称` | 文本 | 保持原值 |
| 4 | `订单号` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零 |
| 5 | `交易日期` | 日期，显示格式`yyyy-mm-dd` | 源值`yyyymmdd`按日期解析后统一输出；无法识别时保留原值 |
| 6 | `交易金额` | 数值 | 保留原数值精度；不添加货币符号或千位分隔符，不使用科学计数法 |
| 7 | `检索参考号` | 文本 | 保留完整原值，不得使用科学计数法或截断 |
| 8 | `模版类型` | 文本 | 保持原值 |
| 9 | `状态` | 文本 | 按第5.7节归并源值；匹配回款明细命中时替换为`已回款` |
| 10 | `描述` | 文本 | 保持原值 |
| 11 | `提交时间` | 日期时间，显示格式`yyyy-mm-dd HH:mm:ss` | 保留到秒；无法识别时保留原值 |
| 12 | `更新时间` | 日期时间，显示格式`yyyy-mm-dd HH:mm:ss` | 保留到秒；无法识别时保留原值 |
| 13 | `终端号` | 文本 | 保留完整原值，不得丢失前导零 |
| 14 | `分店id` | 文本 | 保持原值 |
| 15 | `分店名` | 文本 | 保持原值 |
| 16 | `所在地区` | 文本 | 保持原值 |
| 17 | `详细地址` | 文本 | 保持原值 |
| 18 | `地区编码` | 文本 | 保持原值，不得丢失前导零 |
| 19 | `tel` | 文本 | 保持原值，不得丢失前导零 |
| 20 | `发票号码` | 文本 | 不得使用科学计数法、截断或丢失前导零 |
| 21 | `发票金额` | 文本 | 源值格式不统一（如出现`3000,00`这类非标准小数点写法），原样保留，不解析为数值 |
| 22 | `购买方名称` | 文本 | 保持原值 |
| 23 | `图片1`（第一次出现） | 文本 | 保持原值 |
| 24 | `S/N码` | 文本 | 保留完整原值及其中的字母、数字、空格和符号 |
| 25 | `是否属于 AI 产品` | 文本 | 保持原值 |
| 26 | `图片1`（第二次出现） | 文本 | 保持原值；与第23列按列位置区分，不得互相覆盖 |
| 27 | `图片2` | 文本 | 保持原值 |
| 28 | `图片3` | 文本 | 保持原值 |
| 29 | `图片4` | 文本 | 保持原值 |
| 30 | `img5` | 文本 | 保持原值；“电脑”工作表使用同义名称`图片5`（第5.1节） |
| 31 | `img6` | 文本 | 保持原值 |
| 32 | `img7` | 文本 | 保持原值 |
| 33 | `img8` | 文本 | 保持原值 |
| 34 | `img9` | 文本 | 保持原值 |
| 35 | `img10` | 文本 | 保持原值 |
| 36 | `img11` | 文本 | 保持原值 |
| 37 | `img12` | 文本 | 保持原值 |
| 38 | `img13` | 文本 | 保持原值 |
| 39 | `img14` | 文本 | 保持原值 |
| 40 | `img15` | 文本 | 保持原值 |
| 41 | `签收时间` | 日期时间，显示格式`yyyy-mm-dd HH:mm:ss` | 保留到秒；无法识别时保留原值 |
| 42 | `remark` | 文本 | 保持原值 |
| 43 | `EEG` | 文本 | 保持原值 |
| 44 | `物流单号` | 文本 | 保持原值 |
| 45 | `erpOrderNum` | 文本 | 保持原值 |
| 46 | `ocrModify` | 文本 | 保持原值 |
| 47 | `modifyStatus` | 文本 | 保持原值 |
| 48 | `introduceInvoiceFlag` | 文本 | 保持原值 |
| 49 | `是否交旧` | 文本 | 保持原值 |
| 50 | `是否自提` | 文本 | 保持原值 |
| 51 | `receiverName` | 文本 | 保持原值 |
| 52 | `productCode` | 文本 | 保持原值 |
| 53 | `subsideAmt` | 文本 | 保持原值，不解析为数值 |
| 54 | `productName` | 文本 | 保持原值 |
| 55 | `交旧品类` | 文本 | 保持原值 |
| 56 | `收货地址是否农村地区` | 文本 | 保持原值 |
| 57 | `airConditionerKitInfo` | 文本 | 保持原值；“电脑”工作表没有该字段时留空（第5.1节） |
| 58 | `开票日期` | 日期，显示格式`yyyy-mm-dd` | 源值`yyyymmdd`按日期解析后统一输出；无法识别时保留原值 |
| 59 | `补贴金额` | 数值，显示格式`0.00` | 按第5.7节从回款明细家电电脑（第7节）匹配；未命中时按交易金额估算，无法估算时留空 |

### 5.4 数码已上传数据输出字段及保存格式

数码已上传数据固定输出以下59列，并严格保持名称、顺序和保存格式；前25列与家电、电脑相同，第26列起结构不同；第59列`补贴金额`按第5.7节匹配或估算生成：

| 顺序 | 字段 | Excel保存类型 | 要求 |
|---:|---|---|---|
| 1–25 | 同第5.3节第1–25列 | 同第5.3节 | 同第5.3节 |
| 26 | `IMEI1` | 文本 | 不得使用科学计数法、截断或丢失前导零 |
| 27 | `IMEI2` | 文本 | 不得使用科学计数法、截断或丢失前导零 |
| 28 | `图片1`（第二次出现） | 文本 | 保持原值；与第23列按列位置区分，不得互相覆盖 |
| 29 | `图片2` | 文本 | 保持原值 |
| 30 | `图片3` | 文本 | 保持原值 |
| 31 | `图片4` | 文本 | 保持原值 |
| 32 | `图片5` | 文本 | 保持原值 |
| 33 | `图片6` | 文本 | 保持原值 |
| 34 | `img7` | 文本 | 保持原值 |
| 35 | `img8` | 文本 | 保持原值 |
| 36 | `img9` | 文本 | 保持原值 |
| 37 | `img10` | 文本 | 保持原值 |
| 38 | `img11` | 文本 | 保持原值 |
| 39 | `img12` | 文本 | 保持原值 |
| 40 | `img13` | 文本 | 保持原值 |
| 41 | `img14` | 文本 | 保持原值 |
| 42 | `img15` | 文本 | 保持原值 |
| 43 | `签收时间` | 日期时间，显示格式`yyyy-mm-dd HH:mm:ss` | 保留到秒；无法识别时保留原值 |
| 44 | `remark` | 文本 | 保持原值 |
| 45 | `物流单号` | 文本 | 保持原值 |
| 46 | `erpOrderNum` | 文本 | 保持原值 |
| 47 | `ocrModify` | 文本 | 保持原值 |
| 48 | `modifyStatus` | 文本 | 保持原值 |
| 49 | `introduceInvoiceFlag` | 文本 | 保持原值 |
| 50 | `是否交旧` | 文本 | 保持原值 |
| 51 | `是否自提` | 文本 | 保持原值 |
| 52 | `receiverName` | 文本 | 保持原值 |
| 53 | `productCode` | 文本 | 保持原值 |
| 54 | `subsideAmt` | 文本 | 保持原值，不解析为数值 |
| 55 | `productName` | 文本 | 保持原值 |
| 56 | `oldExchangeType` | 文本 | 保持原值 |
| 57 | `收货地址是否农村地区` | 文本 | 保持原值 |
| 58 | `开票日期` | 日期，显示格式`yyyy-mm-dd` | 源值`yyyymmdd`按日期解析后统一输出；无法识别时保留原值 |
| 59 | `补贴金额` | 数值，显示格式`0.00` | 按第5.7节从回款明细数码（第8节）匹配；未命中时按交易金额估算，无法估算时留空 |

数码已上传数据不输出`EEG`、`交旧品类`和`airConditionerKitInfo`（家电、电脑特有）；家电、电脑已上传数据不输出`IMEI1`、`IMEI2`和`oldExchangeType`（数码特有）。除第5.3、5.4节列出的58个源字段及第5.7节新增的`补贴金额`外，其他未列出的字段不输出。

### 5.5 格式与空值规则

- Excel按规定的数据类型保存。
- `实时清分UUID`、`商户号`、`订单号`、`检索参考号`、`终端号`、`发票号码`、`地区编码`、`tel`、`S/N码`、`IMEI1`及`IMEI2`等标识字段必须保持完整，不得出现科学计数法、截断、精度丢失或前导零丢失。
- `发票金额`和`subsideAmt`按文本原样保留，不解析为数值，不受数值字段规则约束。
- 日期或日期时间无法识别时保留原值，不得猜测修正。
- 数值字段（`交易金额`）保留原正负号和有效小数位，不得因格式转换改变实际数值；为空时保持为空，不得自动填充为`0`。
- 文本字段为空时保持为空，不得写入`null`、`None`或其他占位内容。
- 除日期及日期时间的格式统一外、第5.7节对`状态`的覆盖及`补贴金额`的生成外，保留字段的原始值不得发生其他修改。

### 5.6 输出要求

- 每个数据组分别输出一份XLSX。
- 每个输出文件只包含一个合并后的数据表，且只保留一行表头。

### 5.7 补贴金额匹配、估算与状态覆盖

- 已上传家电电脑与已上传数码分别独立匹配各自对应的回款明细（家电电脑↔第7节回款明细家电电脑，数码↔第8节回款明细数码），不得跨数据组匹配。
- 匹配对象仅限回款明细的“正常区域”：按第7.5、8.5节完成重复记录识别与沉底后，排除粉色`#FFC7CE`填充的沉底/重复记录，只使用未填色的正常记录；参与匹配前须先完成对应回款明细的全部校验（第7、8节）。
- 回款明细工作簿缺失或结构不符合第7.1、8.1节要求时，必须停止生成正式输出并报告异常，不得绕过校验，也不得因此跳过状态覆盖和补贴金额而继续输出其余字段（与第5.1节前25列结构校验同一严格程度）。
- 参与回款匹配前，先按下表对输出字段`状态`的原值做精确文本归并；归并只替换列出的名称，不得包含匹配、模糊匹配或自行扩展，未列出的原值逐字保留：

| 原值 | 归并为 |
|---|---|
| `审核通过` | `审核通过未回款` |
| `同步(已上送)` | `待审核` |
| `暂存` | `待审核` |
| `待同步` | `待审核` |
| `核销失败` | `审核失败` |

- 匹配键按优先级分三级，进入较高优先级并得到唯一命中后，不再尝试较低优先级：
  1. **订单号（主键）**：非空时，与回款明细正常区域的`商户订单号`精确匹配；
  2. **检索参考号（次键）**：仅当订单号为空，或订单号非空但未在回款明细正常区域中找到相同`商户订单号`时才尝试；非空时与回款明细正常区域的`交易参考号`精确匹配；
  3. **发票号码（三键）**：仅当前两级均未命中时才尝试；非空时与回款明细正常区域的`发票号`精确匹配。
- 每一级匹配都对命中记录的`补贴金额`去重：
  - 恰好一个不同值时，视为本级命中，取该值，不再尝试下一级；
  - 两个及以上不同值时，判定为歧义，立即停止，不再尝试下一级，本条记录视为未命中；
  - 未找到相同键值时，视为本级未命中，仅在此情况下才进入下一级。
- 命中时：输出字段`状态`固定覆盖为`已回款`，覆盖上一步的归并结果（即使归并后的值恰好等于其他有效状态，也一律以`已回款`为准）；新增输出字段`补贴金额`填入命中的回款明细金额。
- 未命中时（含三级键均为空、查无匹配，或任一级歧义）：`状态`保持归并后的值（未列入归并表的原值保持源值不变）；`补贴金额`按`交易金额 × 15%`估算，保留两位小数，再按数据组封顶：家电电脑不超过`1500.00`元，数码不超过`500.00`元。估算金额不代表已回款，不得将`状态`改为`已回款`。
- 未命中且`交易金额`为空或无法解析为数值时，无法估算，`补贴金额`留空，不得填`0`；即使匹配键发生歧义，也按上述未命中规则估算，不再尝试低优先级匹配键。
- 本节归并、匹配与估算只修改输出字段`状态`的值、新增输出字段`补贴金额`，不修改回款明细的任何字段，也不得因匹配结果增加、删除、合并或移动已上传数据的明细记录，不影响其他输出字段的生成。

### 5.8 验收要求

- 当前数码输出59列、7,185条明细，其中4,906条命中回款、2,279条使用估算；估算中980条达到`500.00`元封顶。
- 当前家电电脑输出59列、4,825条明细，其中3,875条命中回款、950条使用估算；估算中18条达到`1500.00`元封顶。上述估算记录均有可解析的`交易金额`；源数据变化后重新计算。
- 未命中记录的当前源`状态`分布：数码为`审核通过`2,018、`待审核`184、`核销失败`43、`审核失败`31、`暂存`3；家电电脑为`审核通过`624、`待审核`175、`审核失败`130、`同步(已上送)`17、`核销失败`2、`暂存`1、`待同步`1。输出按第5.7节归并。
- 文件名中的商户号须与数据组及明细中的`商户号`一致；不一致时按第5.1节报错。
- 当前两个数据组的非空`实时清分UUID`均无重复；新增文件后的重复值或重复导出按第5.2节处理。

## 6. 门店银联交易明细处理

### 6.1 输入文件与有效数据范围

- 输入文件位于`输入目录`，文件名结构为`商户号_MX_YYYYMMDDHHMMSS_序号.xlsx`。
- 仅处理商户号为`89813014812B06R`或`89813015722APT1`，且文件名完整符合上述结构的工作簿；时间戳和序号按实际导出文件识别，不得限定当前值。
- 当前源文件包含以下18个工作簿，仅作为核对基准：
  - `89813014812B06R_MX_*.xlsx`：9个；
  - `89813015722APT1_MX_*.xlsx`：9个。
- 文件名以`~$`开头的Excel临时文件不属于输入范围，必须排除。
- 后续新增的匹配工作簿及其中全部工作表必须自动纳入，不得设置固定工作簿数、工作表数或明细行数上限。
- 读取所有匹配工作簿中的全部工作表；当前工作表名称均为`对账数据`。每个工作表第2行必须与第6.2节规定的26个字段名称及顺序完全一致，且须具备本节规定的首行汇总和末行提示结构。
- 任一匹配工作表缺少必需字段、存在重复或错位字段、首尾结构不符合要求，或出现说明页等非交易明细工作表时，必须终止本次处理，不得生成正式输出，并报告文件名、工作表名及具体结构异常。
- 每张源表的有效交易数据为第3行至倒数第2行：
  - 第1行是汇总信息，不属于交易明细；
  - 第2行是字段表头，仅用于确定字段名称和顺序；
  - 最后一行是D1手续费提示，不属于交易明细。
- 第1行中的`总交易金额(元)`、`总交易笔数`、`总手续费(元)`、`T0手续费(元)`、`定期服务费(元)`、`D1手续费(元)`、`总结算金额(元)`及其对应数值均不保留，也不另行生成汇总表。
- 最后一行“请注意：D1手续费字段为预估数据仅供参考，实际以17：40分之后的D1划付数据为准。”整行不保留；正式表头中的`D1手续费`字段仍须保留。

### 6.2 合并规则与输出字段

- 按文件名升序读取工作簿；同一工作簿内按源工作表顺序读取。
- 将所有有效交易数据纵向合并为一个数据表，只保留一行表头。
- 保留全部有效交易记录，不筛选、不去重。
- 两个不同输入文件包含完全相同的汇总行、表头、全部交易明细及末行提示时，视为疑似重复导出，必须终止处理并报告涉及文件；不得自行删除其中一份后继续输出。交易流水号或订单号重复可能由撤销、退货等业务产生，不得仅凭单个标识字段重复直接删除记录。
- 输出字段固定为以下26列，名称及顺序不得变更：

| 顺序 | 字段 | 顺序 | 字段 |
|---:|---|---:|---|
| 1 | `清算时间` | 14 | `发卡行` |
| 2 | `交易时间` | 15 | `商户号` |
| 3 | `终端号` | 16 | `商户名称` |
| 4 | `交易类型` | 17 | `分店简称` |
| 5 | `卡号` | 18 | `商户订单号` |
| 6 | `交易金额` | 19 | `银商订单号` |
| 7 | `清算金额` | 20 | `交易方式` |
| 8 | `手续费` | 21 | `分店` |
| 9 | `T0手续费` | 22 | `优惠金额` |
| 10 | `D1手续费` | 23 | `分期手续费` |
| 11 | `流水号` | 24 | `付款附言` |
| 12 | `检索号` | 25 | `备注` |
| 13 | `卡类型` | 26 | `买家ID` |

### 6.3 退货记录处理

- `交易类型`精确等于`消费撤销`、`消费撤消`或`联机退货`的记录均认定为退货记录；其中源数据当前使用`消费撤消`，同时兼容语义相同的`消费撤销`写法。
- 对每条退货记录，使用其非空`检索号`回溯`交易类型`精确等于`消费`的原消费记录；同一`检索号`恰好命中一条原消费时，该原消费认定为已退货。未命中、`检索号`为空或同号命中多条原消费时不得任选，原消费保持不变。
- 退货记录及其唯一命中的原消费记录，`备注`均统一覆盖为`已退货`，不保留原备注。
- 退货记录及其唯一命中的原消费记录统一移动至其他记录之后；Excel中须将这些记录整行填充为粉色`#FFC7CE`。
- 除上述退货记录及其唯一命中的原消费外，其他记录的`备注`保持原值。
- 最终顺序为“其他记录 → 退货记录及其唯一命中的原消费记录”；两个分组内部均保持合并前的相对顺序，不再重新排序。

### 6.4 字段保存格式

| 顺序 | 字段 | Excel保存类型 | 格式及要求 |
|---:|---|---|---|
| 1 | `清算时间` | 日期，显示格式`yyyy-mm-dd` | 源值`yyyymmdd`按日期解析后统一输出，月、日使用两位数字 |
| 2 | `交易时间` | 日期时间，显示格式`yyyy-mm-dd HH:mm:ss` | 保留到秒 |
| 3 | `终端号` | 文本 | 保留完整原值，不得丢失前导零 |
| 4 | `交易类型` | 文本 | 保持原值 |
| 5 | `卡号` | 文本 | 保留完整掩码内容及`*`字符，不得转换为数值 |
| 6 | `交易金额` | 数值，显示格式`0.00` | 不添加货币符号或千位分隔符，不使用科学计数法 |
| 7 | `清算金额` | 数值，显示格式`0.00` | 不添加货币符号或千位分隔符，不使用科学计数法 |
| 8 | `手续费` | 数值，显示格式`0.00` | 不添加货币符号或千位分隔符，不使用科学计数法 |
| 9 | `T0手续费` | 数值，显示格式`0.00` | 不添加货币符号或千位分隔符，不使用科学计数法 |
| 10 | `D1手续费` | 数值，显示格式`0.00` | 源值如`0.0`统一输出为`0.00`；不添加货币符号或千位分隔符 |
| 11 | `流水号` | 文本 | 保留完整原值，不得丢失前导零 |
| 12 | `检索号` | 文本 | 保留完整原值，不得使用科学计数法或截断 |
| 13 | `卡类型` | 文本 | 保持原值 |
| 14 | `发卡行` | 文本 | 保持原值 |
| 15 | `商户号` | 文本 | 保留完整原值，不得丢失前导零 |
| 16 | `商户名称` | 文本 | 保持原值 |
| 17 | `分店简称` | 文本 | 保持原值 |
| 18 | `商户订单号` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零 |
| 19 | `银商订单号` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零 |
| 20 | `交易方式` | 文本 | 保持原值 |
| 21 | `分店` | 文本 | 保持原值 |
| 22 | `优惠金额` | 数值，显示格式`0.00` | 不添加货币符号或千位分隔符，不使用科学计数法 |
| 23 | `分期手续费` | 数值，显示格式`0.00` | 不添加货币符号或千位分隔符，不使用科学计数法 |
| 24 | `付款附言` | 文本 | 即使内容仅由数字组成，也按文本保留 |
| 25 | `备注` | 文本 | 退货记录统一为`已退货`，其他记录保持原值 |
| 26 | `买家ID` | 文本 | 保留完整原值，不得转换为数值 |

- 日期或日期时间无法识别时保留原值，不得猜测修正。
- 数值字段保留原正负号并统一为两位小数，不得因格式化改变实际金额。
- 数值字段为空时保持为空，不得自动填充为`0`。
- 文本字段为空时保持为空，不得写入`null`、`None`或其他占位内容。

### 6.5 输出文件

合并及处理结果输出为：

- `银联交易明细门店.xlsx`

输出要求：

- 输出文件只包含一个合并后的数据表，且只保留一行表头。
- Excel文件中，退货记录整行填充为粉色`#FFC7CE`。

### 6.6 验收要求

- 必须读取全部符合第6.1节识别规则的工作簿及其全部有效工作表；当前源文件不变时，应读取18个工作簿并合并13,971条有效交易记录。新增文件后按实际工作簿、工作表和有效明细数验收；如数量异常，应先检查文件范围、工作表范围以及首尾非明细行的排除情况，不得直接丢弃数据。
- 输出表必须且只能包含上述26个字段，字段名称与顺序一致，且仅有一行表头。
- 第1行汇总信息及其数值、最后一行D1手续费提示均不得出现在输出文件中；正式字段`D1手续费`必须存在。
- 退货判断必须使用`交易类型`精确匹配；原消费只允许按第6.3节的非空`检索号`唯一命中规则回溯。所有退货记录及其唯一命中的原消费须沉底、`备注`为`已退货`，并在Excel中整行填充为粉色`#FFC7CE`。
- 未沉底记录与沉底记录各自的相对顺序必须与合并前一致。
- 标识类字段不得因格式转换出现前导零丢失、科学计数法、截断或精度变化；空值按本节规则保持为空。

## 7. 家电电脑回款明细处理

### 7.1 输入范围与批次识别

- 输入文件的通用格式为`输入目录/yyyy年以旧换新补贴明细.xlsx`，其中`yyyy`为四位年份；当前年度输入文件为`输入目录/2026年以旧换新补贴明细.xlsx`。
- 每次只处理一个年份的工作簿；该年度的全部批次合并在一起，不同年份的记录不得进入同一合并表、重复组或输出结果。
- 输入目录中同时存在多个年份的匹配文件时，仅选择文件名年份最新的一个，不合并较早年份的文件；最新年份无法唯一确定（如多个文件对应同一最新年份）时，必须终止处理，不得生成正式输出，并报告涉及的文件名。
- 处理当前及以后新增的全部批次明细工作表；名为`汇总`的工作表必须完全排除，不参与字段映射、数据合并或结果输出。
- 当前共有7张批次明细表，合计104,029条有效明细；该数量仅作为当前源文件的验收基准。
- 完成第7.2节字段映射后，仅保留`核销商编`精确等于`89813015722APT1`的明细行；其余门店编码（如同一商户下的其他`89813015722AP*`门店）的记录不参与第7.4节合并、第7.5节去重或最终输出。此过滤按行执行，不影响批次识别本身——工作表是否认定为批次明细表仍按第7.2节字段映射判断，与`核销商编`的具体取值无关。当前104,029条原始有效明细中，`核销商编`精确等于`89813015722APT1`的为3,932条，即为进入第7.4节合并的记录数。
- 每张批次明细表以第1行为表头、第2行起为明细。只有单元格格式而没有实际值的空白行、空白列及空白区域不属于数据。
- 新增工作表能够通过第7.2节的已确认字段名或同义字段名映射出以下19个基础字段时，认定为批次明细表并自动纳入处理：`拨付批次`、`交易参考号`、`商户订单号`、`交易订单号`、`销售企业名称`、`核销商编`、`其他支付`、`销售金额`、`实收销售金额`、`补贴金额`、`补贴比例`、`SN码`、`所在地区`、`商品编码`、`能耗等级`、`编码品类`、`商品名称`、`发票号`、`ID`。
- 新增工作表缺少上述任一基础字段，或使用未确认的新字段名表达相同业务含义时，应记录为待映射异常，不得按列位置或内容猜测，也不得静默混入结果。
- 出现上述待映射异常时，必须终止本次处理，不得生成`回款明细家电电脑.xlsx`正式结果；异常信息须明确列出工作表名称、缺少的基础字段或尚未确认的源字段名，待规则补充后重新处理。

### 7.2 统一表头与字段映射

输出固定使用以下24列统一表头，并严格保持名称和顺序：

| 顺序 | 统一字段名 | 已确认的源字段名 |
|---:|---|---|
| 1 | `拨付批次` | `拨付批次` |
| 2 | `交易完成时间` | `交易完成时间` |
| 3 | `交易参考号` | `交易参考号` |
| 4 | `商户订单号` | `商户订单号` |
| 5 | `交易订单号` | `交易订单号` |
| 6 | `销售企业名称` | `销方名称`或`销售企业名称` |
| 7 | `核销商编` | `商户编号`或`核销商编` |
| 8 | `其他支付` | `其他支付` |
| 9 | `销售金额` | `应收销售金额`或`销售金额` |
| 10 | `实收销售金额` | `实收销售金额` |
| 11 | `补贴金额` | `补贴金额` |
| 12 | `补贴比例` | `补贴比例` |
| 13 | `SN码` | `SN码` |
| 14 | `所在地区` | `所在地区` |
| 15 | `商品编码` | `商品编码` |
| 16 | `能耗等级` | `能耗等级` |
| 17 | `编码品类` | `编码品类` |
| 18 | `商品名称` | `商品名称` |
| 19 | `发票金额` | `发票金额` |
| 20 | `发票号` | `发票号` |
| 21 | `发票头/购买方名称` | `发票头/购买方名称` |
| 22 | `ID` | `ID` |
| 23 | `退回原因` | `退回原因` |
| 24 | `原拨付批次` | `原拨付批次` |

同义字段统一规则：

- `销方名称`统一映射为`销售企业名称`。
- `商户编号`统一映射为`核销商编`。
- `应收销售金额`统一映射为`销售金额`。
- 不同批次中`其他支付`与`销售金额`的列位置不一致，必须按表头名称映射，不得使用固定列号。

当前批次扩展字段的可用情况：

| 当前批次 | `交易完成时间` | `发票金额` | `发票头/购买方名称` | `退回原因` | `原拨付批次` |
|---|---|---|---|---|---|
| 第一批次 | 无，留空 | 无，留空 | 无，留空 | 无，留空 | 无，留空 |
| 第二批次 | 有，直接映射 | 无，留空 | 无，留空 | 无，留空 | 无，留空 |
| `1.1-1.29` | 有，直接映射 | 有，直接映射 | 无，留空 | 无，留空 | 无，留空 |
| 第四批次 | 有，直接映射 | 有，直接映射 | 无，留空 | 无，留空 | 无，留空 |
| 第五批次 | 有，直接映射 | 有，直接映射 | 无，留空 | 无，留空 | 有，直接映射 |
| 第6批次 | 有，直接映射 | 有，直接映射 | 有，直接映射 | 有，直接映射 | 有，直接映射 |
| 第7批次 | 有，直接映射 | 有，直接映射 | 有，直接映射 | 有，直接映射 | 有，直接映射 |

其他字段规则：

- 源表缺少`交易完成时间`、`发票金额`、`发票头/购买方名称`、`退回原因`或`原拨付批次`时，对应输出值留空，不得从相邻列、工作表名称或其他字段推断。
- `交易时间`（仅第一批次使用，与`交易完成时间`含义不同）和`创建时间`（第7批次该列实际写入的是批次标签而非真实时间）不纳入统一表头，也不得误映射到其他字段。
- 新增批次出现统一表头以外的额外字段时，默认不输出，不得因此改变24列统一表头。

### 7.3 字段保存格式

| 顺序 | 字段 | Excel保存类型 | 格式及要求 |
|---:|---|---|---|
| 1 | `拨付批次` | 文本 | 保持原值，不根据工作表名称重新生成 |
| 2 | `交易完成时间` | 文本 | 保持源文本原值，不解析为日期或日期时间类型；源表无该字段时留空 |
| 3 | `交易参考号` | 文本 | 保留完整原值及字母，不得转换为数值或截断 |
| 4 | `商户订单号` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零 |
| 5 | `交易订单号` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零 |
| 6 | `销售企业名称` | 文本 | 保持原值 |
| 7 | `核销商编` | 文本 | 保留完整原值及字母，不得转换为数值或丢失前导零 |
| 8 | `其他支付` | 数值或文本`-`；数值显示格式`0.00` | 可解析的数值保留正负号；源值为`-`时保持`-`，不得改为`0`或空值 |
| 9 | `销售金额` | 数值，显示格式`0.00` | 保留正负号，不添加货币符号或千位分隔符 |
| 10 | `实收销售金额` | 数值，显示格式`0.00` | 保留正负号，不添加货币符号或千位分隔符 |
| 11 | `补贴金额` | 数值，显示格式`0.00` | 保留正负号，不添加货币符号或千位分隔符 |
| 12 | `补贴比例` | 百分比数值，显示格式`0.00%` | 保留底层比例值；例如15%显示为`0.15`对应的百分比 |
| 13 | `SN码` | 文本 | 保留字母、数字、空格、符号及大小写 |
| 14 | `所在地区` | 文本 | 保持原值 |
| 15 | `商品编码` | 文本 | 即使全部由数字组成也按文本保存，不得使用科学计数法或丢失前导零 |
| 16 | `能耗等级` | 文本 | 保持原值 |
| 17 | `编码品类` | 文本 | 保留品类编码、连字符及名称 |
| 18 | `商品名称` | 文本 | 保持原值，包括空格、符号及型号信息 |
| 19 | `发票金额` | 数值，显示格式`0.00` | 保留正负号，不添加货币符号或千位分隔符；源表无该字段时留空 |
| 20 | `发票号` | 文本 | 不得使用科学计数法、截断或丢失前导零 |
| 21 | `发票头/购买方名称` | 文本 | 保持原值；源表无该字段时留空 |
| 22 | `ID` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零 |
| 23 | `退回原因` | 文本 | 保持原值；源表无该字段时留空 |
| 24 | `原拨付批次` | 文本 | 保持原值；源表无该字段时留空 |

通用格式规则：

- `销售金额`、`实收销售金额`、`补贴金额`和`发票金额`按数值保存并统一显示或输出为两位小数，不得使用科学计数法或改变正负号。
- `其他支付`中可解析的数值按数值保存并统一显示或输出为两位小数；源值为`-`时按文本原样保留。该字段允许同时存在数值单元格和文本`-`，不得将`-`转换为`0`、`0.00`或空值。
- `补贴比例`按数值比例保存，不得将`0.15`错误处理为`0.15%`或`15`。
- 除`补贴金额`外，数值字段为空时保持为空，不得自动填充为`0`；`补贴金额`为空时按第7.5节的数据异常规则终止处理。文本字段为空时保持为空，不得写入`null`、`None`或其他占位内容。
- 除`其他支付`中已确认的文本`-`外，任一非空数值字段或`补贴比例`无法按本节要求解析时，必须终止本次处理，不得生成正式输出，并明确报告工作表名称、源数据行号、字段名称和原始值。
- 本节文本类型字段（含必需字段，如`核销商编`）遇到Excel错误值（如`#N/A`，常见于源表查找公式未匹配，例如`核销商编`）时，按原样文本保留（如`#N/A`），不视为数据异常、不终止处理；此为文本字段对第1.3节错误值规则的例外，数值字段（`其他支付`、`销售金额`、`实收销售金额`、`补贴金额`、`发票金额`、`补贴比例`）不适用该例外，仍按本节规则终止处理。
- 除同义字段改名和显示格式统一外，不得擅自清洗、修正或补全字段值。

### 7.4 数据合并

1. 按源工作簿中的工作表排列顺序读取所有已识别的批次明细表。
2. 每张表内部按原数据行顺序读取，第1行仅用于识别表头，第2行起读取有效明细。
3. 按第7.2节映射为24列统一结构。
4. 按第7.1节规则，剔除`核销商编`不等于`89813015722APT1`的明细行，再将剩余明细依次纵向追加。
5. 合并结果只保留一行统一表头。
6. 除第4步的门店编码过滤外，保留全部有效明细，不作其他筛选、删除或汇总。
7. 结果记录数应等于所有纳入处理的批次明细中`核销商编`等于`89813015722APT1`的行数之和，不得使用固定行数限制新增批次。

### 7.5 重复记录识别与沉底

- 必须先完成第7.4节规定的全部批次字段映射与数据合并，再对完整的24列合并结果统一执行本节规则。
- 不得在单个工作表或单个批次内预先完成重复判断后再合并；重复组必须在全部已纳入批次的记录之间统一识别，包括跨批次出现的重复记录。

#### 7.5.1 分组依据

每条记录按以下优先级选择首个非空字段作为分组依据：

1. `交易订单号`
2. `商户订单号`
3. `发票号`

具体要求：

- 三个字段均为空时，该记录不参与重复判断，按普通记录处理。
- 只有两条或以上记录的依据字段类型相同且字段值完全相同，才组成重复组。
- 不同字段类型不得交叉匹配。例如，某条记录使用的`商户订单号`不得与另一条记录的`交易订单号`直接匹配。
- 空单元格、空字符串及纯空格均视为空；判断空值时可忽略首尾空格，但不得修改输出字段原值。

#### 7.5.2 补贴金额汇总与处理

- 未形成重复组的单条记录不参与组内求和；其中`补贴金额`小于`0`的记录直接沉底并整行填充为粉色`#FFC7CE`，`补贴金额`大于或等于`0`的记录保持在正常区域。三个分组字段均为空的记录同样按此单条规则处理。
- 对每个重复组内全部记录的`补贴金额`按数值求和：
  - 合计小于或等于`0`：该组全部记录沉底，并在Excel中整行填充为粉色`#FFC7CE`。
  - 合计大于`0`：仅保留该组最后一次出现的记录在正常区域，其余记录全部沉底并在Excel中整行填充为粉色`#FFC7CE`。
- “最后一次出现”以第7.4节的合并顺序为准，不按日期、订单号或其他字段重新排序。
- 最终顺序为“未沉底记录 → 粉色沉底记录”，两个区域内部均保持合并后的原相对顺序。
- 本规则只改变记录位置和Excel整行颜色，不修改`补贴金额`、订单号、发票号、退回原因或其他字段值。
- `补贴金额`为空或无法解析为数值时，不得按`0`参与求和。必须终止本次处理，不得生成正式输出，并明确报告对应的工作表名称、源数据行号、字段名称和原始值，待确认后重新处理。

当前数据核对基准（按第7.1节`核销商编`过滤后的3,932条明细统计）：

- 3,932条明细的`交易订单号`均非空，因此当前全部使用`交易订单号`分组，未触发备用字段。
- 共有22个重复组，涉及53条记录，最大一组3条。
- 13个重复组的`补贴金额`合计小于或等于`0`，共26条记录全部沉底。
- 9个重复组的`补贴金额`合计大于`0`，共18条非末次记录沉底。
- 另有12条未形成重复组且`补贴金额`小于`0`的单条记录沉底。
- 当前预计共56条记录沉底并在Excel中填充为粉色`#FFC7CE`；新增批次或过滤后的明细变化后必须重新计算。

### 7.6 输出文件

处理结果输出为：

- `回款明细家电电脑.xlsx`

输出要求：

- 输出文件只包含一个合并后的数据表及一行24列表头。
- Excel中按第7.5节沉底的记录须整行填充为粉色`#FFC7CE`。
- 本次处理的年度结果写入第1.1节规定的`输出目录/`，文件名不附加年份。

### 7.7 验收要求

- 当前源文件不变时，按第7.1节`核销商编`过滤后输出应有3,932条明细，其中56条按第7.5节沉底；新增批次或过滤后的明细变化后重新计算。
- 按`核销商编`过滤后的3,932条明细中，`交易完成时间`有245条为空、`交易参考号`有14条为空、`其他支付`有470条为空、`所在地区`有80条为空、`发票金额`有470条为空、`发票头/购买方名称`有1,991条为空、`退回原因`和`原拨付批次`各有3,910条为空；`商品编码`和`商品名称`当前均无空值。这些源空值在输出中必须继续保持为空，不得填充为`0`、`null`、`None`或其他占位内容。
- 上述空值数量仅为当前验收基准，源数据或过滤结果变化后重新统计。

## 8. 数码回款明细处理

### 8.1 输入范围与批次识别

- 输入文件的通用文件名格式为`yyyy年数码补贴明细.xlsx`，其中`yyyy`为四位年份。
- 当前输入文件为`输入目录/2026年数码补贴明细.xlsx`。
- 每次只处理一个年份的工作簿；该年度的全部批次合并在一起，不同年份的记录不得进入同一合并表、重复组或输出结果。
- 输入目录中同时存在多个年份的匹配文件时，仅选择文件名年份最新的一个，不合并较早年份的文件；最新年份无法唯一确定（如多个文件对应同一最新年份）时，必须终止处理，不得生成正式输出，并报告涉及的文件名。
- 文件名必须完整符合上述格式；以`~$`开头的Excel临时文件及其他相似名称的文件不纳入处理。
- 文件内容为对应年份的数码补贴各批次回款明细；处理范围不得仅限于当前已有批次，后续新增批次也应纳入。
- 不得硬编码当前批次数量或当前工作表名称；新增批次须按本节后续规定的批次识别规则动态识别。
- 名为`汇总`的工作表必须完全排除，不参与批次识别、字段映射、数据合并、统计或结果输出。
- 除`汇总`表外，工作表能够按第8.3节已确认的源字段名或同义字段名映射出以下13个基础字段时，认定为批次明细表并自动纳入处理：`拨付批次`、`交易参考号`、`商户订单号`、`销售企业名称`、`核销商编`、`销售金额`、`补贴金额`、`SN码`、`所在地区`、`商品编码`、`编码品类`、`商品名称`、`发票号`。
- 新增工作表缺少上述任一基础字段，或使用尚未确认的新字段名表达相同业务含义时，应记录为待映射异常，不得按列位置或内容猜测，也不得静默混入合并结果。
- 出现上述待映射异常时，必须终止本次处理，不得生成`回款明细数码.xlsx`正式结果；异常信息须明确列出工作表名称、缺少的基础字段或尚未确认的源字段名，待规则补充后重新处理。
- 经上述规则识别的批次明细表，其有效数据范围按本节后续要求确定，不得自行推断。
- 完成第8.3节字段映射后，仅保留`核销商编`（含同义字段）精确等于`89813014812B06R`的明细行；其余门店编码（如同一商户下的其他`89813014812B0*`门店）的记录不参与第8.5节合并、第8.6节去重或最终输出。此过滤按行执行，不影响批次识别本身——工作表是否认定为批次明细表仍按第8.3节字段映射判断，与`核销商编`的具体取值无关。当前63,737条原始有效明细中，`核销商编`（或同义字段）精确等于`89813014812B06R`的为4,908条，即为进入第8.5节合并的记录数。

### 8.2 当前工作表与表头

当前工作簿包含4个批次明细工作表和1个`汇总`工作表。以下内容仅记录当前文件结构，用作核对基准，不构成固定批次范围；新增批次后应按实际工作表更新。

以下3个工作表使用相同的20列表头：

- `过渡期第一批次（1.1.-1.29）`
- `2026年数码和智能商品过渡期第二批次（1.1.-1.6）`
- `2026年数码和智能商品过渡期第三批次（1.1.-1.29）`

表头按列顺序如下：

1. `拨付批次`
2. `交易时间`
3. `参考号`
4. `商户订单号`
5. `订单号`
6. `销方名称`
7. `经销商编号`
8. `应收销售金额`
9. `其他支付`
10. `实收销售金额`
11. `补贴销售金额`
12. `品类`
13. `商品明细`
14. `SN码`
15. `IMEI1码`
16. `IMEI2码`
17. `商品编码`
18. `所在地区`
19. `发票号码`
20. `备注`

工作表`2026数码线下-提交时间(6.1-7.31)-第五批`使用24列表头，按列顺序如下：

1. `端口情况`
2. `拨付批次`
3. `提交时间`
4. `商户订单号`
5. `参考号`
6. `销售企业名称`
7. `门店名称/县域编码`
8. `商户编号`
9. `核销日期`
10. `商品编码`
11. `销售金额`
12. `补贴金额`
13. `补贴比例/补贴限额`
14. `SN码`
15. `原始SN码`
16. `券号`
17. `所在地区`
18. `类别`
19. `商品名称`
20. `发票号码`
21. `物流单号`
22. `备注`
23. `ID`
24. `客户购买品类`

以下为已明确排除的工作表`汇总`的当前有名表头，仅用于记录源文件结构，不参与任何处理：

1. `数码活动商户编号`
2. `商户名称（四级）`
3. `2026年数码和智能商品过渡期第一批次（1.1.-1.29）`
4. `2026年数码和智能商品过渡期第二批次（1.1.-1.6）`
5. `2026年数码和智能商品过渡期第三批次（1.1.-1.29）`
6. `2026数码线下-提交时间(6.1-7.31)-第五批`
7. `第四批`
8. `第5-1批`
9. `第六批次`
10. `第七批次`
11. `第八批次`
12. `第九批次`
13. `第十批次`
14. `第十一批次`
15. `第十二批次`
16. `第十三批次`
17. `第十四批次`
18. `第十五批次`
19. `第16-18批次（11.21-12.18）`
20. `第十九批次（12.19-12.25）`
21. `第二十批次（25.12.26-26.1.14）`
22. `总计`

`汇总`表的`总计`列之前另有一个无表头的空白分隔列，该列不属于上述有名字段。

### 8.3 统一表头与字段映射

处理结果固定使用以下24列统一表头，并严格保持字段名称和顺序：

| 顺序 | 统一字段名 | 已确认的源字段名 |
|---:|---|---|
| 1 | `拨付批次` | `拨付批次` |
| 2 | `交易完成时间` | `交易完成时间` |
| 3 | `交易参考号` | `参考号`或`交易参考号` |
| 4 | `商户订单号` | `商户订单号` |
| 5 | `交易订单号` | `订单号`或`交易订单号` |
| 6 | `销售企业名称` | `销方名称`或`销售企业名称` |
| 7 | `核销商编` | `经销商编号`、`商户编号`或`核销商编` |
| 8 | `其他支付` | `其他支付` |
| 9 | `销售金额` | `应收销售金额`或`销售金额` |
| 10 | `实收销售金额` | `实收销售金额` |
| 11 | `补贴金额` | `补贴销售金额`或`补贴金额` |
| 12 | `补贴比例` | `补贴比例/补贴限额`或`补贴比例` |
| 13 | `SN码` | `SN码` |
| 14 | `所在地区` | `所在地区` |
| 15 | `商品编码` | `商品编码` |
| 16 | `能耗等级` | `能耗等级` |
| 17 | `编码品类` | `品类`、`类别`或`编码品类` |
| 18 | `商品名称` | `商品明细`或`商品名称` |
| 19 | `发票金额` | `发票金额` |
| 20 | `发票号` | `发票号码`或`发票号` |
| 21 | `发票头/购买方名称` | `发票头/购买方名称` |
| 22 | `ID` | `ID` |
| 23 | `退回原因` | `退回原因` |
| 24 | `原拨付批次` | `原拨付批次` |

当前各批次按以下规则映射：

| 统一输出字段 | 第一、第二、第三批次源字段 | 第五批源字段 |
|---|---|---|
| `拨付批次` | `拨付批次` | `拨付批次` |
| `交易完成时间` | 无，留空 | 无，留空 |
| `交易参考号` | `参考号` | `参考号` |
| `商户订单号` | `商户订单号` | `商户订单号` |
| `交易订单号` | `订单号` | 无，留空 |
| `销售企业名称` | `销方名称` | `销售企业名称` |
| `核销商编` | `经销商编号` | `商户编号` |
| `其他支付` | `其他支付` | 无，留空 |
| `销售金额` | `应收销售金额` | `销售金额` |
| `实收销售金额` | `实收销售金额` | 无，留空 |
| `补贴金额` | `补贴销售金额` | `补贴金额` |
| `补贴比例` | 无，留空 | `补贴比例/补贴限额` |
| `SN码` | `SN码` | `SN码` |
| `所在地区` | `所在地区` | `所在地区` |
| `商品编码` | `商品编码` | `商品编码` |
| `能耗等级` | 无，留空 | 无，留空 |
| `编码品类` | `品类` | `类别` |
| `商品名称` | `商品明细` | `商品名称` |
| `发票金额` | 无，留空 | 无，留空 |
| `发票号` | `发票号码` | `发票号码` |
| `发票头/购买方名称` | 无，留空 | 无，留空 |
| `ID` | 无，留空 | `ID` |
| `退回原因` | 无，留空 | 无，留空 |
| `原拨付批次` | 无，留空 | 无，留空 |

当前源文件所有批次均无`交易完成时间`、`能耗等级`、`发票金额`、`发票头/购买方名称`、`退回原因`和`原拨付批次`对应源字段，这6列当前全部留空；后续批次出现已确认同名字段时按第7节相同规则直接映射。

字段映射要求：

- 必须依据工作表表头名称映射字段，不得按固定列号映射。
- 同一业务字段使用上表所列不同名称时，统一映射为对应的统一字段名。
- 源工作表缺少某个统一字段及其已确认同义字段时，对应输出值留空，不得从相邻列、工作表名称或其他字段推断。
- 当前源文件中的`交易完成时间`、`能耗等级`、`发票金额`、`发票头/购买方名称`、`退回原因`和`原拨付批次`均无对应源字段，当前记录的这6列全部留空；后续批次出现已确认的同名字段时直接映射。
- 后续新增批次应逐列检查表头，并按“统一字段名—已确认的源字段名”表进行映射；不得因为工作表名称或列位置与当前批次相似而套用当前批次的固定列号。
- 后续批次使用尚未确认的新字段名表达相同业务含义时，应记录为待映射异常，不得自行认定为同义字段或静默写入结果。
- `交易时间`、`提交时间`、`门店名称/县域编码`、`核销日期`、`原始SN码`、`券号`、`IMEI1码`、`IMEI2码`、`物流单号`、`备注`、`端口情况`和`客户购买品类`不纳入统一表头，也不得误映射到其他字段。
- 后续批次出现统一表头以外的额外字段时，默认不输出，不得因此改变24列统一表头。

### 8.4 字段保存格式

| 顺序 | 字段 | Excel保存类型 | 格式及要求 |
|---:|---|---|---|
| 1 | `拨付批次` | 文本 | 保持源值，不根据工作表名称重新生成 |
| 2 | `交易完成时间` | 文本 | 保持源文本原值，不解析为日期或日期时间类型；源表无对应字段时留空 |
| 3 | `交易参考号` | 文本 | 保留完整原值及字母，不得转换为数值或截断 |
| 4 | `商户订单号` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零 |
| 5 | `交易订单号` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零；源表无对应字段时留空 |
| 6 | `销售企业名称` | 文本 | 保持原值 |
| 7 | `核销商编` | 文本 | 保留完整原值及字母，不得转换为数值或丢失前导零 |
| 8 | `其他支付` | 数值或文本`-`；数值显示格式`0.00` | 可解析的数值保留正负号；源值为`-`时保持`-`，不得改为`0`或空值 |
| 9 | `销售金额` | 数值，显示格式`0.00` | 保留正负号；不添加货币符号或千位分隔符 |
| 10 | `实收销售金额` | 数值，显示格式`0.00` | 保留正负号；不添加货币符号或千位分隔符；源表无对应字段时留空 |
| 11 | `补贴金额` | 数值，显示格式`0.00` | 保留正负号；不添加货币符号或千位分隔符 |
| 12 | `补贴比例` | 百分比数值，显示格式`0.00%` | 保留底层比例值；例如15%显示为`0.15`对应的百分比；源表无对应字段时留空 |
| 13 | `SN码` | 文本 | 保留字母、数字、空格、符号及大小写 |
| 14 | `所在地区` | 文本 | 保持原值 |
| 15 | `商品编码` | 文本 | 即使全部由数字组成也按文本保存，不得使用科学计数法或丢失前导零 |
| 16 | `能耗等级` | 文本 | 保持原值；源表无对应字段时留空 |
| 17 | `编码品类` | 文本 | 保留品类编码、连字符及名称 |
| 18 | `商品名称` | 文本 | 保持原值，包括空格、符号及型号信息 |
| 19 | `发票金额` | 数值，显示格式`0.00` | 保留正负号；不添加货币符号或千位分隔符；源表无对应字段时留空 |
| 20 | `发票号` | 文本 | 不得使用科学计数法、截断或丢失前导零 |
| 21 | `发票头/购买方名称` | 文本 | 保持原值；源表无对应字段时留空 |
| 22 | `ID` | 文本 | 不得使用科学计数法、截断、四舍五入或丢失前导零；源表无对应字段时留空 |
| 23 | `退回原因` | 文本 | 保持原值；源表无对应字段时留空 |
| 24 | `原拨付批次` | 文本 | 保持原值；源表无对应字段时留空 |

通用格式要求：

- 除`交易完成时间`按原始文本保留外，其余统一输出字段中不包含日期或日期时间类型；源表中的`交易时间`、`提交时间`和`核销日期`不参与输出，也不得映射到其他字段。
- `销售金额`、`实收销售金额`、`补贴金额`和`发票金额`按数值保存并统一显示或输出为两位小数，不得使用科学计数法或改变正负号。
- `其他支付`中可解析的数值按数值保存并统一显示或输出为两位小数；源值为`-`时按文本原样保留。该字段允许同时存在数值单元格和文本`-`，不得将`-`转换为`0`、`0.00`或空值。
- `补贴比例`按数值比例保存，不得将`0.15`错误处理为`0.15%`或`15`。
- 除`补贴金额`外，数值字段为空时保持为空，不得自动填充为`0`；`补贴金额`为空时按第8.6节的数据异常规则终止处理。文本字段为空时保持为空，不得写入`null`、`None`或其他占位内容。
- 除`其他支付`中已确认的文本`-`外，任一非空数值字段或`补贴比例`无法按本节要求解析时，必须终止本次处理，不得生成正式输出，并明确报告工作表名称、源数据行号、字段名称和原始值。
- 本节文本类型字段（含必需字段，如`核销商编`）遇到Excel错误值（如`#N/A`，常见于源表查找公式未匹配，例如`核销商编`）时，按原样文本保留（如`#N/A`），不视为数据异常、不终止处理；此为文本字段对第1.3节错误值规则的例外，数值字段（`销售金额`、`实收销售金额`、`补贴金额`、`发票金额`、`补贴比例`及`其他支付`中的数值部分）不适用该例外，仍按本节规则终止处理。
- 除已确认的表头映射和显示格式统一外，不得擅自清洗、修正、补全或推断字段值。

### 8.5 数据合并

排除第8.1节规定的`汇总`表后，按第7.4节的合并规则处理全部已识别批次；字段映射使用第8.3节，第8.1节的`核销商编`过滤按第7.4节第4步同样方式执行（目标值为`89813014812B06R`），重复记录处理使用第8.6节。只有格式而无实际值的区域不计入明细。

当前数据核对基准（过滤前，按批次统计的原始有效明细）：

| 工作表 | 当前有效明细数 |
|---|---:|
| `过渡期第一批次（1.1.-1.29）` | 466 |
| `2026年数码和智能商品过渡期第二批次（1.1.-1.6）` | 329 |
| `2026年数码和智能商品过渡期第三批次（1.1.-1.29）` | 38,265 |
| `2026数码线下-提交时间(6.1-7.31)-第五批` | 24,677 |
| **合计** | **63,737** |

按第8.1节`核销商编`过滤后，实际进入本节合并及后续处理的明细为4,908条；各批次对该数字的具体贡献随过滤结果而定，不再单独按批次统计。

### 8.6 重复记录识别与沉底

先完成第8.5节合并，再对全部明细统一识别重复组，包括跨批次记录。

#### 8.6.1 分组依据

每条记录按以下优先级选择首个非空字段作为分组依据：

1. `交易参考号`
2. `商户订单号`
3. `发票号`

分组的空值、字段类型和值的判定与第7.5.1节相同；两组数据只在上述字段优先级上不同。

#### 8.6.2 补贴金额汇总与处理

按第7.5.2节的同一规则汇总组内`补贴金额`、决定沉底与粉色填充；“最后一次出现”以第8.5节合并顺序为准。空值或非数值同样须报错，不得按`0`求和。

当前数据核对基准（按第8.1节`核销商编`过滤后的4,908条明细统计）：

- 4,908条明细的`交易参考号`均非空，因此全部按第一优先级使用`交易参考号`分组，未触发`商户订单号`或`发票号`备用字段。
- 当前共有1个重复组，2条记录。
- 该重复组的`补贴金额`合计等于`0`，因此这2条记录全部沉底，并在Excel中整行填充为粉色`#FFC7CE`。

新增批次或过滤后的明细变化后重新计算重复组与沉底数量。

### 8.7 输出文件

合并及处理结果输出为：

- `回款明细数码.xlsx`

输出要求：

- 输出文件只包含一个处理后的数据表，并且只保留一行24列统一表头。
- Excel文件中，按第8.6节沉底的记录须整行填充为粉色`#FFC7CE`。
- 本次处理的年度结果写入第1.1节规定的`输出目录/`，文件名不附加年份。

### 8.8 验收要求

- 当前源文件不变时，按第8.1节`核销商编`过滤后输出应有4,908条明细，其中2条按第8.6节沉底；源数据、过滤结果或分组规则变化后重新计算。
- 过滤后的4,908条明细中，`其他支付`保留文本`-`的有18条，须原样保留；其他可解析的金额值按规定的数值类型和两位小数格式输出，其中1,802条值为数值`0`，应输出为`0.00`。

## 9. 收款单统计处理

### 9.1 输入文件与有效数据范围

- 输入文件位于`输入目录/收款单统计.xlsx`，文件名固定为`收款单统计.xlsx`，不得使用通配规则匹配其他相似文件。
- 输入工作簿必须且只能包含一个工作表；当前工作表为`Sheet1`，第1行为标题行、第2行为表头、第3行起为数据区域。
- 第2行必须与第9.2节规定的60个字段名称及顺序完全一致，不得缺失、重复、增加或错位。
- 必须删除第1行标题，不得将标题内容作为表头或明细输出。
- 必须保留第2行作为字段表头。
- 最后一个实际有值行是合计行，必须排除，不得作为明细或另行输出汇总数据。
- 最后一个实际有值行的第1列必须为`合计`；不符合时视为源表结构异常，不得自行把其他行当作合计行删除。
- 有效明细范围为第3行至最后一个实际有值行的前一行；不得依据Excel格式化范围或固定末行号读取。
- 当前源文件不变时，有效明细为第3行至第20,603行，共20,601条。
- 工作表数量、标题行、表头或末行合计结构不符合上述要求时，必须终止处理，不得生成正式输出，并报告具体结构异常。

### 9.2 源表字段

源表第2行共包含60个字段，按列顺序如下：

1. `日期`
2. `销售部门`
3. `单据号`
4. `联系人`
5. `联系电话`
6. `销售金额`
7. `客户名称`
8. `收款日期`
9. `销售类别`
10. `编号`
11. `销售员`
12. `预定`
13. `服务方式`
14. `制单机器`
15. `收款机器`
16. `制单员`
17. `收款员`
18. `手工票号`
19. `摘要`
20. `联营商`
21. `商品名称`
22. `商品简码`
23. `计量单位`
24. `库存类型`
25. `价格类型`
26. `是否负卖`
27. `数量`
28. `尾款金额`
29. `定金金额`
30. `实收手续费`
31. `实收包装及配件款`
32. `其它费用`
33. `费用承担供应商`
34. `商家贴卡金额`
35. `厂家贴卡金额`
36. `销售折让金额`
37. `已记账`
38. `会员卡号`
39. `预定单号`
40. `预定单日期`
41. `原票号`
42. `是否作废`
43. `作废人`
44. `作废日期`
45. `兑现使用积分`
46. `积分`
47. `积分兑现金额`
48. `认筹码`
49. `手机号`
50. `接口同步成功`
51. `是否已出库`
52. `交易流水号`
53. `退货赠品销售金额`
54. `认证类型`
55. `认证码`
56. `已上传`
57. `线上支付`
58. `记账错误信息`
59. `新券码`
60. `Crm会员账号`

### 9.3 输出字段与映射

处理结果仅保留以下6个字段，并严格保持名称和顺序：

| 顺序 | 输出字段 | 源字段或生成方式 |
|---:|---|---|
| 1 | `日期` | 源字段`日期` |
| 2 | `单据号` | 源字段`单据号` |
| 3 | `商品名称` | 源字段`商品名称` |
| 4 | `原票号` | 源字段`原票号` |
| 5 | `匹配单据号` | 根据源字段`日期`和`单据号`生成 |
| 6 | `备注` | 新增字段 |

- 除上述6个字段外，其他源字段均不输出；源字段`销售类别`和`数量`不保留。
- `备注`不是源表字段，须按第9.5节规则赋值；不满足该节任何赋值条件时保持为空，不得从`摘要`、`记账错误信息`或其他源字段推断内容。
- 新增`匹配单据号`和`备注`字段不得增加、删除或合并明细记录。

### 9.4 匹配单据号

`匹配单据号`由源字段`日期`和`单据号`组合生成，格式为`yymmdd单据号`，日期与单据号直接拼接，不添加空格或其他分隔符。

生成规则：

1. 将源字段`日期`解析为日期，并转换为六位`yymmdd`格式；例如`2026-01-01`转换为`260101`。
2. 将源字段`单据号`按文本读取；如果值以`收款`二字开头，仅删除开头的`收款`，其余内容保持不变。没有该前缀时直接使用原值。
3. 将六位日期与处理后的单据号直接拼接。例如，`日期=2026-01-01`、`单据号=收款ZFFX000003`时，生成`260101ZFFX000003`。
4. `匹配单据号`必须按文本保存，不得转换为数值、科学计数法或丢失前导零。
5. `日期`或`单据号`为空，或日期无法识别时，`匹配单据号`留空，不得从`收款日期`、`预定单日期`、`作废日期`或其他字段推断。

生成`匹配单据号`只新增字段值，不得修改输出中的原`日期`或`单据号`。

### 9.5 备注生成与关联匹配

#### 9.5.1 唯一关联条件

本节所有关联匹配均使用同一个条件：一条记录的非空`原票号`与另一条记录的非空`匹配单据号`完全相同，即：

`原票号 = 匹配单据号`

具体要求：

- 匹配时只比较上述两个字段的完整值，不附加被匹配记录的`销售类别`、日期、商品名称或其他字段条件。
- `销售类别`只用于决定哪些记录需要生成或修改`备注`，不得作为两个字段是否匹配命中的判断条件。
- 比较方向不改变匹配关系：既可由特殊记录的`原票号`查找对应记录的`匹配单据号`，也可由正常销售记录的`匹配单据号`反查特殊记录的`原票号`。
- 两个非空值必须完全相同才算命中，不得使用包含、模糊、部分或近似匹配，也不得补全、截断或改写字段值后再比较。
- 任一侧为空时不参与匹配，多个空值之间不得判定为命中。
- 同一个值命中一条或多条记录均属于匹配命中；多条命中不得导致源记录被复制、合并或删除。

#### 9.5.2 备注初始赋值

先根据源字段`销售类别`精确赋值：

| `销售类别`原值 | `备注`初始值 |
|---|---|
| `退货` | `退货-退单` |
| `零售补差` | `零售补差` |
| `同型号换货` | `同型号换货` |

- 未列出的其他`销售类别`不自动生成备注，`备注`保持为空。
- `销售类别`判断必须使用完整值精确匹配，不得使用包含匹配、模糊匹配或自行扩展同义值。

#### 9.5.3 零售补差和同型号换货记录

完成全部记录的`匹配单据号`生成及备注初始赋值后，对`销售类别`精确等于`零售补差`或`同型号换货`的记录执行以下处理：

1. 读取该记录的非空`原票号`。
2. 按第9.5.1节的唯一关联条件，在全部有效明细的`匹配单据号`中查找相同值；被查找记录的`销售类别`不限。
3. 找到一条或多条匹配记录时，判定为匹配命中，并将发起匹配的`零售补差`或`同型号换货`记录本身的`备注`覆盖为`补差/同型号换货`。
4. 被匹配到的记录不因本次匹配修改`备注`或其他字段。
5. 未匹配命中时，不覆盖初始备注：`零售补差`记录仍为`零售补差`，`同型号换货`记录仍为`同型号换货`。
6. `原票号`为空时不参与匹配，并保留该记录的初始备注。

#### 9.5.4 正常销售记录

完成第9.5.3节的特殊记录处理后，再对`销售类别`精确等于`正常销售`的记录执行以下处理：

1. 读取该正常销售记录的非空`匹配单据号`。
2. 按第9.5.1节的唯一关联条件，在全部有效明细的`原票号`中反查相同值；查找本身不预先限定被查找记录的`销售类别`。
3. 检查所有命中记录在第一阶段处理完成后的最终`备注`。
4. 只要至少一条命中记录的最终`备注`精确等于`退货-退单`或`补差/同型号换货`，就将该正常销售记录本身的`备注`设置为`退货-原单`。
5. 其余未匹配命中，或虽有匹配但命中记录的最终备注均不属于上述两个值的正常销售记录，`备注`必须保持为空，不得填入其他内容。
6. 同一正常销售记录命中多条符合条件的特殊记录时，仍只将其备注设置一次`退货-原单`，不得重复记录或复制该行。
7. 被命中的退货、零售补差或同型号换货记录不因第二阶段匹配再次修改备注或其他字段。

#### 9.5.5 记录保留与处理边界

上述处理只允许修改规则指定记录的新增字段`备注`，不得修改`原票号`、`匹配单据号`或任何源字段值，也不得增加、删除、合并或移动记录。

相同`匹配单据号`可能对应同一单据中的多条商品明细。所有明细必须完整保留，不得按`匹配单据号`去重、合并或只保留一条；匹配和备注赋值按每条明细分别执行。

当前数据核对基准：

- 重复`匹配单据号`组共253个，涉及517条明细，最大一组4条。
- `退货`记录共2,875条，备注均为`退货-退单`。
- `零售补差`记录共40条，其中12条匹配命中并将备注覆盖为`补差/同型号换货`，其余28条备注保持为`零售补差`。
- `同型号换货`记录共28条，当前全部匹配命中，备注均覆盖为`补差/同型号换货`。
- 当前预计共有40条记录的备注为`补差/同型号换货`；源数据变化后必须重新计算，不得沿用固定数量。
- `正常销售`记录共17,658条，其中2,508条在第二阶段匹配命中并将备注设置为`退货-原单`：2,488条命中了备注为`退货-退单`的记录，20条命中了备注为`补差/同型号换货`的记录。
- 其余15,150条正常销售记录不满足`退货-原单`赋值条件，备注保持为空。
- 同一正常销售记录即使命中多条特殊记录，也只计为一条`退货-原单`记录；当前共有70条正常销售记录命中多条符合条件的特殊记录。

### 9.6 字段保存格式

| 顺序 | 字段 | Excel保存类型 | 格式及要求 |
|---:|---|---|---|
| 1 | `日期` | 日期，显示格式`yyyy-mm-dd` | 保留源日期，不使用`收款日期`或其他日期字段替代 |
| 2 | `单据号` | 文本 | 保留完整源值，包括开头的`收款`二字；不得转换为数值、科学计数法或丢失前导零 |
| 3 | `商品名称` | 文本 | 保持原值，包括空格、符号、品牌及型号信息 |
| 4 | `原票号` | 文本 | 保留完整原值，不得转换为数值、科学计数法、截断或丢失前导零 |
| 5 | `匹配单据号` | 文本 | 按第9.4节生成并完整保存，不得转换为数值、科学计数法、截断或丢失前导零 |
| 6 | `备注` | 文本 | 按第9.5节生成；不满足任何赋值条件时保持为空 |

通用格式要求：

- 输出中没有数值字段；源表中的金额、数量及其他数值字段均不输出。
- `日期`为空时保持为空；非空日期应按有效日期解析并统一为`yyyy-mm-dd`，无法识别时不得猜测修正，应记录为数据异常并停止生成正式输出。
- 文本字段为空时保持为空，不得写入`0`、`null`、`None`或其他占位内容。
- 除第9.4节生成`匹配单据号`和第9.5节生成`备注`外，不得清洗、改写、补全或推断其他输出字段值。

### 9.7 输出文件

处理结果输出为：

- `收款单统计.xlsx`

输出要求：

- 输出文件只包含一个处理后的数据表，并且只保留一行6列表头。
- 所有明细保持源表第3行至合计行前一行的原相对顺序；生成匹配字段和备注不得重新排序或沉底记录。
- 输出文件必须写入`输入目录`以外的目录，避免与只读输入文件`输入目录/收款单统计.xlsx`发生覆盖。

### 9.8 验收要求

- 输入工作簿必须且只能包含一个工作表，第2行必须完整匹配第9.2节规定的60个字段，标题行和最后一个实际有值的合计行不得出现在输出中。
- 输出必须且只能包含`日期`、`单据号`、`商品名称`、`原票号`、`匹配单据号`和`备注`6个字段，并严格保持该顺序及一行表头。
- 当前源文件不变时，输出文件应包含20,601条明细；处理前后不得增加、删除、合并或去重记录。
- 当前253个重复`匹配单据号`组及其517条明细必须全部保留，明细顺序不得改变。
- 当前最终备注分布应为：空值15,150条、`退货-退单`2,875条、`退货-原单`2,508条、`补差/同型号换货`40条、`零售补差`28条，合计20,601条。
- 所有日期、文本、空值和标识字段必须符合第9.6节的保存格式；`单据号`、`原票号`和`匹配单据号`不得出现科学计数法、截断或前导零丢失。

## 10. 销售用券情况统计处理

### 10.1 输入文件与当前结构

- 输入文件名称固定为`销售用券情况统计.xlsx`，位于`输入目录/销售用券情况统计.xlsx`；文件名不包含日期、批次、序号或其他可变后缀，不得使用通配规则匹配其他相似文件。
- 文件内容为销售用券情况统计数据。
- 输入工作簿当前只包含一个工作表，工作表名称为`Sheet1`。
- 第1行为标题行，标题为`销售用券情况统计`，并合并`A1:AB1`；处理时必须去掉该行，不得作为表头或明细输出。
- 第2行为源字段表头；输出仅保留第10.3节规定的一行11列表头。第3行起为明细数据。
- 最后一个实际有值行是合计行，第1列为`合计`；处理时必须去掉该行，不得作为明细输出，也不得另行输出汇总行。
- 当前源文件最后一个实际有值行为第16,621行：第1行是标题、第2行是表头、第3行至第16,620行是明细、第16,621行是合计，共16,618条有效明细。
- 有效明细范围必须根据实际表头和末行合计结构确定，不得将当前固定行号作为以后文件的永久读取范围。
- 输入工作簿必须且只能包含一个工作表；第1行第1列必须为`销售用券情况统计`，第2行必须与第10.2节的28个源字段名称及顺序完全一致，不得缺失、重复、增加或错位。最后一个实际有值行的第1列必须为`合计`；不得凭固定行号或单元格格式化范围猜测末行。
- 工作表数量、标题、表头或末行合计结构不符合上述要求时，必须终止处理，不得生成正式输出，并报告具体异常位置；不得自行把其他行当作标题、表头或合计行删除。
- 处理顺序固定为：校验输入结构并排除标题和合计 → 读取全部有效明细 → 生成`补贴额`、`数量`、`匹配单据号`、`参考号`、`数电发票号码`及`备注` → 按第10.7节保存字段 → 按第10.12节沉底与填色 → 按第10.8和10.9节输出及验收。不得自行增加筛选、去重或其他处理。

### 10.2 源表字段

源表第2行共包含28个字段，按列顺序如下：

1. `供应商`
2. `原始供应商`
3. `单据号`
4. `单据日期`
5. `收款员`
6. `商品名称`
7. `商品简码`
8. `品牌`
9. `销售部门`
10. `销售员`
11. `业绩成本`
12. `业绩利润`
13. `价格类型名称`
14. `库存类型`
15. `财务大类`
16. `顾客姓名`
17. `备注`
18. `明细摘要`
19. `收款日期`
20. `销售成本`
21. `不含券收入`
22. `其它`
23. `本期尾款`
24. `销售单价`
25. `含券收入`
26. `2026家电国补（计入收入）`
27. `2026数码国补（计入收入）`
28. `合计`

### 10.3 输出字段与映射

处理结果仅保留以下11个字段，并严格保持名称和顺序：

| 顺序 | 输出字段 | 源字段或生成方式 |
|---:|---|---|
| 1 | `单据号` | 源字段`单据号` |
| 2 | `单据日期` | 源字段`单据日期` |
| 3 | `商品名称` | 源字段`商品名称` |
| 4 | `品牌` | 源字段`品牌`，按第10.11节归并部分品牌名称 |
| 5 | `财务大类` | 源字段`财务大类`，按第10.11节归并部分类别名称 |
| 6 | `补贴额` | 源字段`合计` |
| 7 | `数量` | 根据输出字段`补贴额`的数值正负生成 |
| 8 | `参考号` | 从源字段`明细摘要`中提取符合规则的参考号 |
| 9 | `数电发票号码` | 按输出字段`匹配单据号`在发票明细（第3节）中查找对应记录，见第10.10节 |
| 10 | `匹配单据号` | 根据源字段`单据日期`和`单据号`生成 |
| 11 | `备注` | 按输出字段`匹配单据号`在收款单统计（第9节）中查找对应记录，见第10.12节 |

- 除上述11个字段外，其他源字段均不输出，但可以按照本节明确规则用于生成`补贴额`、`数量`、`参考号`、`数电发票号码`、`匹配单据号`或`备注`。

### 10.4 补贴额与数量

- 输出字段`补贴额`直接取源字段`合计`的明细值，不使用最后一行合计数据，也不得自行改用`2026家电国补（计入收入）`或`2026数码国补（计入收入）`。
- 输出字段`数量`根据同一条记录的`补贴额`数值生成：
  - `补贴额 > 0`时，`数量`为`1`；
  - `补贴额 = 0`时，`数量`为`0`；
  - `补贴额 < 0`时，`数量`为`-1`。
- `补贴额`为空或无法解析为数值时，不得按`0`生成数量，应记录为数据异常，待确认后处理。
- 当前源字段`合计`的部分数值带有Excel浮点计算尾差，例如`299.85000000000002`。按第10.7节统一到两位小数后再确定输出金额；判定阈值以第10.7节为准，不得另行设定。当前实测尾差远小于该阈值，未出现影响正负号或`数量`的情况；源数据变化后须重新确认，不得默认尾差一定不影响`数量`。以后若出现无法解释为两位小数金额的源值，不得静默舍入，应报告异常并待确认。

### 10.5 匹配单据号

`匹配单据号`由源字段`单据日期`和`单据号`组合生成，格式为`yymmdd单据号`，日期与单据号直接拼接，不添加空格或其他分隔符。

生成规则：

1. 将源字段`单据日期`解析为日期，并转换为六位`yymmdd`格式；例如`2026-08-29`转换为`260829`。
2. 将源字段`单据号`按文本读取；如果值以`收款`二字开头，仅删除开头的`收款`，其余内容保持不变。没有该前缀时直接使用原值。
3. 将六位日期与处理后的单据号直接拼接。例如，`单据日期=2026-08-29`、`单据号=收款0023000002`时，生成`2608290023000002`。
4. `匹配单据号`必须按文本保存，不得转换为数值、科学计数法或丢失前导零。
5. `单据日期`或`单据号`为空时，`匹配单据号`留空，不得从`收款日期`或其他字段推断；非空日期无法识别时按第10.7节记录异常并停止生成正式输出。

生成`匹配单据号`只新增字段值，不得修改输出中的原`单据日期`或`单据号`。

### 10.6 参考号提取

`参考号`从同一条记录的源字段`明细摘要`中提取，最终非空值必须为`[0-9]{11}N`，即11位数字加大写`N`，例如`16867252734N`。

#### 10.6.1 权威校验集

- 仅使用第6.1节规定的两组门店银联交易明细工作簿中全部有效交易行的源字段`检索号`。完整识别当前及后续新增文件，排除首行汇总、表头及末行提示；重复`检索号`按一个值看待。
- 只有符合`[0-9]{11}N`的`检索号`可进入校验集，不符合该格式的值一律不纳入，也不得使用第5节的`检索参考号`或第7、8节的`交易参考号`替代。
- 当前核对基准：18个门店银联工作簿共13,971条有效交易，`检索号`均非空；不同值13,630个，其中13,614个符合本节格式，其余16个以`W`结尾因而落选。源数据变化后重新计算，不得硬编码这些数量。
- 权威文件缺失、源结构异常或无法完整读取时，必须停止生成正式输出并报告异常，不得绕过校验。

#### 10.6.2 提取与修正顺序

按以下优先级处理每条`明细摘要`，进入较高优先级并得到有效值后，不再用较低优先级候选覆盖：

1. **原样提取**：在摘要全文中查找完整的`[0-9]{11}N`片段。片段前后可为汉字、空格或标点，但不得从更长的数字串或连续字母数字串中截取。仅保留与权威校验集完全相同的值；格式正确但未命中的值仍不得输出。
2. **生成数字候选**：若无已校验的原样值，逐一读取摘要中长度为10至12位的完整连续数字串，不得截取较长数字串的中间部分。若摘要包含`N`、`n`、`W`、`w`、`M`、`m`、`H`、`h`、`B`或`b`，且按原顺序提取的全部数字总长度为10至12位，可额外将这些数字顺序连接为一个候选，以识别误夹空格或标点的情况；否则不得跨片段拼接。
3. **零数字改动**：对11位数字候选仅补齐或纠正末尾大写`N`，与权威校验集完全匹配后列为有效候选。
4. **单次数字改动**：仅当第3步无有效候选时，对10至12位数字候选执行至多一次单个数字插入、删除或替换，再补上大写`N`并与权威校验集完全匹配。不得修改两个及以上数字，或仅凭相似度保留未命中的值。

每个优先级内先对命中的权威值去重：恰好一个不同值时输出该值；两个及以上不同值时`参考号`留空并记录歧义，不得任选；没有命中时进入下一优先级。`明细摘要`为空或只有`预售`、`倒票`等非编号内容时留空，不得从其他字段推断。提取与修正只改变新增输出字段`参考号`，不得改写源字段`明细摘要`；同一参考号出现在多条明细中不得因此去重、合并或删除记录。

#### 10.6.3 保存与核对

- `参考号`按文本保存，保留11位数字、前导零和末尾大写`N`，不得转换为数值、科学计数法或截断。
- 当前16,618条销售明细中，12,974条原样提取并通过校验；381条经唯一候选修正后通过校验，其中286条原先未能按合规格式提取、95条原先格式正确但数字错一位；最终13,355条有参考号、3,263条为空。当前有1条异常值对应两个不同权威候选，按歧义规则留空；源数据变化后重新计算，不得硬编码这些数量。
- `单据日期`与银联`交易时间`、`补贴额`与银联`优惠金额`仅作复核信息，不作为参考号校验的硬性条件。当前95条单次数字替换修正中有18条日期不相同；原样参考号已命中的记录中有74条金额不相同，不得因此擅自删除或改写参考号。

### 10.7 字段保存格式

| 顺序 | 字段 | Excel保存类型 | 格式及要求 |
|---:|---|---|---|
| 1 | `单据号` | 文本 | 保留源值及开头的`收款`二字；不得截断或丢失前导零 |
| 2 | `单据日期` | 日期，显示格式`yyyy-mm-dd` | 解析源日期后统一格式，不用`收款日期`替代 |
| 3 | `商品名称` | 文本 | 保留完整名称、符号和型号，不得改写 |
| 4 | `品牌` | 文本 | 按第10.11节归并列出的品牌名称，其余保留源值 |
| 5 | `财务大类` | 文本 | 按第10.11节归并列出的类别名称，其余保留源值 |
| 6 | `补贴额` | 数值，显示格式`0.00` | 取源字段`合计`，保留正负号；不加货币符号、千位分隔符或科学计数法 |
| 7 | `数量` | 整数，显示格式`0` | `1`、`0`或`-1`；按第10.4节由`补贴额`生成，不保留小数位 |
| 8 | `参考号` | 文本 | 按第10.6节提取、修正及校验；保留前导零和末尾大写`N` |
| 9 | `数电发票号码` | 文本 | 按第10.10节匹配；不得转为数值或科学计数法，不得截断或丢失前导零 |
| 10 | `匹配单据号` | 文本 | 按第10.5节生成；保留完整内容和前导零 |
| 11 | `备注` | 文本 | 按第10.12节匹配；不满足任何命中条件时固定填`未上传`，不保持为空 |

- 源表当前16,618条明细的`单据号`、`单据日期`、`商品名称`、`品牌`、`财务大类`及`合计`均非空；源文件变化后仍须逐行核查，不能用当前情况代替空值规则。
- `单据日期`非空但无法识别为有效日期时，不得猜测或改用其他日期，应记录异常并停止生成正式输出；空日期保持为空，所生成的`匹配单据号`亦为空。
- `补贴额`使用十进制金额处理。源值与最接近的两位小数之差不超过`0.000001`元时，认定为浮点尾差并按该两位小数值输出；超过该阈值时记录异常，不得擅自舍入。`数量`依据处理后的两位小数金额正负生成。
- 文本字段为空时保持为空，不得写成`0`、`null`、`None`等占位内容。
- 除本节明确的日期格式、金额浮点尾差处理、`数量`、`参考号`、`数电发票号码`、`匹配单据号`、`备注`生成及第10.11节`品牌`/`财务大类`归并外，不得清洗或修改其他输出字段值。

### 10.8 输出文件

处理结果输出为：

- `销售用券情况统计.xlsx`

输出要求：

- 输出文件只包含一个处理后的数据表，并且只保留一行11列表头；源表第1行标题和最后一行合计均不得出现。
- 明细按第10.12.4节沉底与填色，不删除或合并记录。
- 输出文件必须写入`输入目录`以外的目录，避免与只读输入文件`输入目录/销售用券情况统计.xlsx`发生覆盖。

### 10.9 验收要求

- 当前源文件不变时，输出应为11列、16,618条明细。
- 当前全部明细日期有效、单据号均以`收款`开头，生成的`匹配单据号`无重复；如源数据变化，按第10.5和10.7节重新核对，不得沿用当前结论。
- 当前按两位小数处理后的`补贴额`合计为`6,531,556.47`元，与源表末行`合计`列金额一致；当前正金额14,946条、负金额1,672条、零金额0条，对应的`数量`必须分别为`1`、`-1`、`0`。源数据变化后重新计算这些核对值。
- `参考号`按第10.6节核对；当前7,350条命中`数电发票号码`，按第10.10节核对；`备注`与沉底分布按第10.12节核对。源数据变化后重新计算这些数量。

### 10.10 数电发票号码匹配

- 输出字段`数电发票号码`按输出字段`匹配单据号`（第10.5节）与发票明细（第3节）的输出字段`匹配单据号`进行精确文本匹配，两者完全相同才视为命中。
- 匹配对象为发票明细全部有效记录，含第3.5节分类的正常、重复及异常三类记录，不因分类而排除。
- 参与匹配前须先按第3.4节规则从发票明细生成`匹配单据号`；发票明细工作簿缺失、文件名日期无法唯一确定或结构不符合第3.1节要求时，必须停止生成正式输出并报告异常，不得绕过校验，也不得因此跳过`数电发票号码`而继续输出其余字段。
- 一条销售用券明细的`匹配单据号`为空时，`数电发票号码`留空，不得从其他字段推断。
- 一条销售用券明细的`匹配单据号`非空时，在发票明细中查找相同的非空`匹配单据号`：
  - 对应的`数电发票号码`去重后恰好一个不同值时，输出该值；
  - 对应的`数电发票号码`去重后为两个及以上不同值时，判定为歧义，`数电发票号码`留空，不得任选；
  - 未找到相同`匹配单据号`，或找到但对应`数电发票号码`均为空时，`数电发票号码`留空。
- 匹配只生成`数电发票号码`，不修改发票明细，也不改变销售用券明细数量或顺序。

### 10.11 品牌与财务大类归并

- 归并只按下表列出的原值做精确文本替换，不得包含匹配、模糊匹配或自行扩展未列出的名称；未列出的原值必须逐字保留源值。
- 只归并输出字段`品牌`、`财务大类`，不修改源数据或其他输出字段。

`品牌`归并：

| 原值 | 归并为 |
|---|---|
| `COLMO厨热JX` | `美的` |
| `美的厨热JX` | `美的` |
| `东芝JX` | `美的` |
| `华凌` | `美的` |
| `小天鹅` | `美的` |
| `COLMO` | `美的` |
| `卡萨帝` | `海尔` |
| `统帅` | `海尔` |
| `晶弘` | `格力` |

`财务大类`归并：

| 原值 | 归并为 |
|---|---|
| `国产彩电` | `彩电` |
| `进口彩电` | `彩电` |
| `新业务类` | `数码` |

### 10.12 备注匹配、沉底与填色

`备注`分三个阶段生成：第一阶段与收款单统计匹配；仍未命中时，第二阶段改用参考号、数电发票号码与已上传数据匹配；两个阶段均未命中时，第三阶段固定填`未上传`。三个阶段均只新增输出字段`备注`的值，不修改任何源数据或其他输出字段，也不改变明细数量。

#### 10.12.1 第一阶段：收款单统计匹配

- 输出字段`备注`按输出字段`匹配单据号`（第10.5节）与收款单统计（第9节）的输出字段`匹配单据号`进行精确文本匹配，两者完全相同才视为命中；只使用已生成的`匹配单据号`，不得改用`单据号`、`单据日期`或其他字段直接对照收款单统计。
- 参与匹配前须先按第9.4和9.5节规则从收款单统计生成`匹配单据号`及`备注`；收款单统计工作簿缺失或结构不符合第9.1节要求时，必须停止生成正式输出并报告异常，不得绕过校验，也不得因此跳过`备注`而继续输出其余字段（与第10.6.1节门店银联权威源、第10.10节发票明细同一严格程度）。
- 一条销售用券明细的`匹配单据号`为空时，本阶段不命中，进入第10.12.2节。
- 一条销售用券明细的`匹配单据号`非空时，在收款单统计中查找相同的非空`匹配单据号`，先剔除空`备注`（空值不参与去重判定），再对剩余的非空`备注`去重：
  - 恰好一个不同值时，输出该值，本阶段命中，不再进入第10.12.2节；
  - 两个及以上不同值时，判定为歧义，本阶段不命中，仍进入第10.12.2节尝试匹配；
  - 未找到相同`匹配单据号`，或找到但对应`备注`均为空时，本阶段不命中，进入第10.12.2节。
- 收款单统计中`匹配单据号`重复（同一单据的多条商品明细，见第9.5.5节）不影响销售用券情况统计的明细数量，也不得据此对收款单统计去重、合并或修改。

#### 10.12.2 第二阶段：已上传数据兜底匹配

- 仅对第10.12.1节未命中的记录执行本阶段；已在第一阶段命中的记录不得被本阶段结果覆盖。
- 匹配对象为已上传家电电脑、已上传数码（第5节）合并后的全部有效明细，两个数据组一并查找，不按`财务大类`等字段区分数据组；两个数据组任一缺失或结构不符合第5.1节要求时，必须停止生成正式输出并报告异常，不得绕过校验，也不得因此跳过`备注`而继续输出其余字段。
- 匹配键按优先级分两级，进入较高优先级并得到唯一命中后，不再尝试较低优先级：
  1. **参考号（主键）**：非空时，与已上传数据的`检索参考号`（第5.3、5.4节第7列）精确匹配；
  2. **数电发票号码（次键）**：仅当参考号为空，或参考号非空但未在已上传数据中找到相同`检索参考号`时才尝试；非空时与已上传数据的`发票号码`（第5.3、5.4节第20列）精确匹配。
- 每一级匹配都先剔除空`状态`（第5.3、5.4节第9列），再对剩余非空`状态`去重：
  - 恰好一个不同值时，输出该值为`备注`，本阶段命中，不再尝试下一级；
  - 两个及以上不同值时，判定为歧义，本阶段判定为未命中，立即停止，不再尝试下一级（不降级到数电发票号码）；
  - 未找到相同键值，或找到但对应`状态`均为空时，视为本级未命中，仅在此情况下才进入下一级。
- 参考号和数电发票号码均为空，或均未命中且不构成歧义时，本阶段不命中，进入第10.12.3节。

#### 10.12.3 第三阶段：未命中固定标记

- 第一、第二阶段均未命中时（含空键、查无匹配、任一阶段歧义），`备注`固定填写`未上传`，不再保持为空、不得使用其他占位文字。

#### 10.12.4 沉底与填色

- 第一阶段实际命中的记录沉底并整行填充粉色`#FFC7CE`；第二阶段实际命中及未命中记录在前、不填色。两组各自保持源相对顺序。
- 当前16,618条明细中第一阶段3,355条命中并沉底填粉色；第二阶段兜底11,884条与未命中1,379条保持在前且不填色；源数据变化后重新计算，不得硬编码这些数量。
